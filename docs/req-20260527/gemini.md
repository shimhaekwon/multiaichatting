```python?code_reference&code_event_index=2
import os

# Define the content for 10-기본-고객요구.md
content_10 = """# [개요] LLM 다중 라운드 토론 시스템 (v2.0)

본 문서는 사용자가 제안한 크롬 확장 프로그램 기반의 'LLM 다중 라운드 토론 시스템' 개발을 위한 개요 서명서입니다. 개발 원칙에 의거하여 추정치 없이 요구사항을 명확하게 요약했습니다.

## 1. 시스템 핵심 목적
사용자가 크롬 브라우저 로그인 세션을 기반으로 3대 LLM(Claude, ChatGPT, Gemini)을 연동하여, 다중 라운드(기본 3회, 최대 9회) 방식을 통해 상호 교차 검증된 최종 답변을 도출하는 로컬 전용 브라우저 확장 프로그램입니다.

## 2. 주요 기능 요약
* **LLM 세션 연동:** API 키 없이 크롬 브라우저의 기존 로그인 세션을 자동으로 탐색 및 활용합니다. (ChatHub 오픈소스 기반 개선)
* **다중 라운드 토론 규칙:** * **R1 (1라운드):** 타 LLM의 답변을 모른 채 독립적으로 응답 (Blind)
  * **R2 (2라운드):** 타 LLM의 1라운드 답변 전체를 검토 후 상호 의견 제시
  * **R3 (3라운드) ~ 최대 9라운드:** 최종 답변 도출 및 사용자 제어에 따른 회차 증감/연장/조기종료
* **사용자 제어 및 개입:** 라운드 간 대기 시간에 LLM 변경, 개별 1:1 재질의, 다음 회차 질문 사전 입력이 가능합니다. 단, LLM 응답 중에는 개입이 불가합니다.
* **로컬 데이터 관리:** 서버 없이 사용자의 브라우저 내 IndexedDB에 라운드별로 자동 저장되며, 전체 이력 조회 및 Markdown(종합/LLM별 개별) 파일 내보내기를 지원합니다.

## 3. 기술 제약 및 환경
* **플랫폼:** Chrome 브라우저 전용 확장 프로그램 (Manifest V3 규격)
* **지원 OS:** Windows, macOS, Linux (모바일 미지원)
* **아키텍처:** 단일 사용자용, 로컬 전용 아키텍처 (서버 저장 없음, API 키 미사용, 암호화 미적용)
* **출력 제한:** 응답 컨텍스트는 1,500 토큰으로 제한하며 구조화된 형식으로 출력을 강제하여 장황함을 회피합니다.
"""

# Define the content for 20-핵심-고객요구.md
content_20 = """# [핵심] LLM 다중 라운드 토론 시스템 상세 설계서 (v2.0)

전문 개발자가 아키텍처 수립 및 핵심 모듈을 구현할 수 있도록 상세화된 기술 요구사항 명세서입니다.

## 1. 아키텍처 및 기술 스택
* **Base Source:** `E:\\workspace\\chathub` 개선 및 확장
* **Platform:** Chrome Extension Manifest V3
* **Storage:** 브라우저 내장 IndexedDB (로컬 전용, 외부 서버 통신 없음)
* **Target OS:** Windows, macOS, Linux 크롬 데스크톱 환경

## 2. 핵심 모듈별 요구사항

### 2.1 LLM Session & Authentication Manager
* **탐색 매커니즘:** Claude, ChatGPT, Gemini 웹 서비스의 크롬 쿠키 및 세션 정보 자동 탐색 (API Key 미사용)
* **예외 처리 워크플로우:** 탐색 실패 시 -> 크롬 로그인 유도 UI 노출 -> 유효 계정 부재 시 default LLM 계정 생성 요청 가이드라인 안내
* **상태 유지:** 사용자가 선택한 LLM 목록(최소 2개 이상 필수)은 세션 간 영구 유지

### 2.2 Round Control Engine (토론 프로세스 제어)
* **라운드 범위:** 기본 3회 ~ 최대 9회 (사용자가 라운드 도중 1회씩 증감 가능, 마지막 회차에서도 연장 가능)
* **동기화 제어:** 선택된 모든 LLM의 Response가 완전히 수신(동기화)된 후 다음 라운드 진입 가능
* **라운드별 상태 머신 (State Machine):**
  * **Round 1:** Blind 모드. 각 LLM에 유저 원본 Prompt 투입 (타 LLM 응답 차단)
  * **Round 2:** 1라운드에 수신된 모든 LLM의 응답 텍스트 전체를 결합하여 각 LLM의 Context에 주입 후 상호 검토 유도
  * **Round 3 ~ N:** 최종 답변 유도 또는 사용자 연장에 따른 반복 제어
* **제어 명령:** 사용자 판단에 의한 조기 종료 및 회차 완료 후 연장 기능 구현

### 2.3 User Interaction & Interruption Policy
* **개입 차단:** Request 전송 후 모든 선택 LLM의 Response가 완료될 때까지 입력창 및 제어 UI 비활성화
* **라운드 간 개입 (Inter-round State):** 라운드와 라운드 사이 대기 상태에서만 아래 기능 허용
  * 참여 LLM 추가/제외
  * 특정 LLM 대상 1:1 재질의 (해당 결과는 다음 라운드 컨텍스트에 포함)
* **사전 입력 버퍼:** LLM이 연산 중인 상태에서 다음 회차에 전송할 request 미리 입력 가능 (단, 전송 버튼은 비활성화)
* **MVP 제외 항목:** 선응답 LLM과의 개별 '몰래 채팅' 기능은 MVP 이후로 배제 (구현 금지)

### 2.4 Timeout & Robustness Manager
* **타임아웃 제어:** 기본 5분, 분 단위로 사용자 커스텀 설정 가능
* **재시도 메커니즘:** LLM 응답 실패 시 3~9회 범위 내에서 사용자가 설정한 횟수만큼 재시도 프로세스 수행
* **프롬프트 메타데이터 주입:** 각 라운드 Request 시 프롬프트 내에 `[제한시간] + [현재차수/제한차수]` 정보를 메타데이터 형태로 강제 삽입하여 LLM에 전달

### 2.5 Data Persistence & Storage Subsystem
* **자동 저장:** 각 라운드가 완료될 때마다 IndexedDB에 즉시 트랜잭션 처리
* **검색 엔진:** 년월일시, 참여 LLM, 라운드 번호를 복합 인덱스로 하는 검색 기능 구현
* **File Export:** 토론 완료 후 전체 컨텍스트 통합 Markdown 파일 생성 기능 및 LLM별 독립적 Markdown 파일 개별 저장 기능 구현

### 2.6 Response Formatting & Guardrails
* **구조화 출력 강제:** LLM 프롬프트 인젝션을 통해 출력 형식을 강제 지정하고, 장황한 출력을 회피하도록 페이로드 설계
* **컨텍스트 가드:** Response context를 1,500 토큰 이하로 제한하는 필터링 또는 가이드라인 설계 검토 필요
"""

# Define the content for 30-종합-고객요구.md
content_30 = """# [종합] LLM 다중 라운드 토론 시스템 가이드라인 (v2.0)

비전문가 및 협업 부서원도 시스템의 모든 동작 프로세스와 화면 구성을 직관적으로 이해하고 검증할 수 있도록 작성된 종합 안내서입니다. 개발 원칙에 따라 임의의 추정 기능은 배제되었습니다.

## 1. 시스템 개요 및 범위
이 시스템은 사용자가 크롬 브라우저에서 실행하는 프로그램(확장 프로그램 Manifest V3)입니다. 별도의 서버나 유료 API 키 없이, 사용자가 기존에 로그인해 둔 ChatGPT, Claude, Gemini 계정을 그대로 활용합니다. 데이터는 유출 없이 사용자의 PC(IndexedDB)에만 저장됩니다.

## 2. 상세 기능 및 사용자 시나리오별 동작 방식

### 2.1 프로그램 시작 및 LLM 연결 (1단계)
* **자동 탐색:** 프로그램을 켜면 Claude, ChatGPT, Gemini의 로그인 상태를 자동으로 확인합니다.
* **로그인 실패 시 대응:**
  1. 로그인이 안 되어 있다면 "크롬에서 해당 서비스에 로그인해 주세요"라는 안내 창을 띄웁니다.
  2. 그래도 연결 가능한 LLM이 없다면, "기본(Default) LLM 계정을 생성해 주세요"라고 친절하게 안내합니다.
* **구현 기준:** 기존 `E:\\workspace\\chathub` 코드를 기반으로 이 탐색 레이아웃을 개선하여 구현합니다.

### 2.2 LLM 선택 및 채팅 준비 (2단계)
* **선택 화면:** 연결된 LLM 목록이 화면에 표시되며, 사용자는 체크박스를 통해 토론에 참여시킬 AI들을 선택합니다.
* **제약 조건:** 최소 2개 이상의 AI를 선택해야만 토론을 시작할 수 있습니다. (AI 1개 단독 실행은 불가능합니다.)
* **기억 기능:** 한 번 선택해 둔 AI 목록은 프로그램을 껐다 켜도 그대로 유지됩니다.

### 2.3 토론 진행 규칙 (3단계: 라운드 규칙)
토론은 기본적으로 3번의 라운드(회차) 동안 진행되며, 최대 9라운드까지 확장할 수 있습니다. 모든 AI가 답변을 끝내야만 다음 라운드로 넘어갑니다.
* **1라운드 (R1 - 블라인드):** AI들은 서로의 존재를 모른 채, 사용자가 처음 입력한 질문에 대해 각자 독립적으로 답변합니다.
* **2라운드 (R2 - 교차 검토):** AI들에게 "방금 전 1라운드에서 나온 다른 AI들의 답변 전체 텍스트"를 복사해서 전달합니다. 각 AI는 타사 AI의 의견을 검토한 후 자신의 의견을 다시 제시합니다.
* **3라운드 (R3 - 최종 답변):** 이전 라운드 의견들을 종합하여 최종 결론 답변을 도출합니다.
* **라운드 제어:** * 사용자는 토론 도중 언제라도 "조기 종료" 버튼을 눌러 끝낼 수 있습니다.
  * 지정된 회차가 끝나면 자동 종료되지만, 필요에 따라 회차를 "연장"할 수 있습니다. 회차는 채팅 중에 1회씩 늘리거나 줄일 수 있으며, 마지막 라운드 안에서도 회차 증설이 가능합니다.

### 2.4 사용자 개입 및 돌발 행동 통제 (4단계)
* **입력 통제:** 질문을 던진 후(Request), 선택된 모든 AI의 답변이 화면에 다 나올 때까지(Response 완료 전) 사용자는 채팅창을 조작하거나 개입할 수 없습니다.
* **라운드 사이 개입:** 라운드가 끝난 대기 상태에서는 아래의 행동이 가능합니다.
  * 토론 도중 특정 AI를 추가하거나 제외하기
  * 특정 AI 하나와 1:1로 다시 질문하기 (이 대화 내용은 다음 라운드 진행 시 전체 컨텍스트에 포함되어 반영됩니다.)
* **사전 입력 기능:** AI들이 열심히 답변을 작성하고 있는 동안, 사용자는 다음 라운드에 보낼 질문을 입력창에 미리 타이핑해 둘 수 있습니다. (단, 전송 버튼은 답변이 끝날 때까지 작동하지 않습니다.)
* **주의(구현 금지):** 먼저 답변을 끝낸 AI와 몰래 개별 채팅을 나누는 기능은 MVP(초기 버전) 이후로 미루며, 본 버전에서는 구현하지 않습니다.

### 2.5 타임아웃 및 재시도 규칙 (5단계)
* **대기 시간 제한:** 기본적으로 AI의 답변을 기다리는 시간은 5분입니다. 사용자가 분 단위로 직접 수정할 수 있습니다.
* **재시도 횟수:** AI가 응답에 실패할 경우 3회에서 9회 사이로 사용자가 지정한 만큼 자동으로 다시 시도합니다.
* **AI 전달 정보:** 질문을 보낼 때 시스템은 내부적으로 `[제한시간] + [현재 라운드 번호 / 총 제한 라운드 번호]` 정보를 질문과 함께 AI에게 전달하여 시간 관리를 인지시킵니다.

### 2.6 데이터 저장 및 이력 조회 (6단계)
* **자동 저장:** 각 라운드가 끝날 때마다 사용자의 브라우저 내부 저장소(IndexedDB)에 대화 내용이 실시간 자동 저장됩니다. 외부 서버로는 절대 전송되지 않습니다.
* **검색 기능:** 저장된 과거 대화들은 `년월일시`, `참여한 LLM 이름`, `라운드 번호` 항목을 통해 간편하게 검색할 수 있습니다.
* **파일 내보내기 (MD 저장):** 토론이 끝나면 전체 토론 내용이 합쳐진 Markdown(.md) 파일과, 각 AI별 답변만 따로 모은 개별 Markdown 파일을 PC에 저장할 수 있습니다.

### 2.7 응답 형식 제약 (7단계)
* **구조화된 출력:** AI들이 중구난방으로 답변하지 않도록 출력 형식을 강제 지정합니다.
* **글자수 제한:** 답변 내용이 너무 길어지지 않도록 응답 컨텍스트는 1,500 토큰 이내로 제한합니다. (개발 시 상세 설계 검토가 필요한 항목입니다.)
* **간소화:** AI 특유의 장황하고 불필요한 서론/결론 미사여구를 회피하도록 조치합니다.

### 2.8 환경 및 기술 제약 사항 (8단계)
* **웹 브라우저:** 구글 크롬(Chrome) 데스크톱 버전 전용입니다. 모바일 브라우저는 지원하지 않습니다.
* **확장 프로그램 규격:** 크롬의 최신 보안 표준인 Manifest V3 규격을 따릅니다.
* **사용 인원:** PC당 단일 사용자만 사용하는 로컬 프로그램입니다.
* **보안 및 서버:** 외부 서버 저장은 없으며, API 키를 쓰지 않고 로컬에만 저장되므로 별도의 데이터 암호화는 적용되지 않습니다.

## 3. 개발 행동 원칙
1. **Step by Step:** 단계별로 차근차근 개발을 진행합니다.
2. **No Guess:** 요구사항 정의서에 기재되지 않은 내용이나 사용자의 의도를 임의로 추측하여 기능을 추가하거나 구현하지 않습니다.
3. **4대 원칙:** 빠짐없이, 틀림없이, 다름없이, 더함없이 요구사항 그대로 명확하게 구현합니다.
4. **자산화:** 차후 재사용이 가능하도록 설계 템플릿을 구조화하여 자산화합니다.
"""

# Write files to disk
with open("10-기본-고객요구.md", "w", encoding="utf-8") as f:
    f.write(content_10)

with open("20-핵심-고객요구.md", "w", encoding="utf-8") as f:
    f.write(content_20)

with open("30-종합-고객요구.md", "w", encoding="utf-8") as f:
    f.write(content_30)

print("Files created successfully.")


```
```text?code_stdout&code_event_index=2
Files created successfully.

```
전달해주신 확정 요구사항 명세서 v2.0을 토대로 개발 원칙(`step by step`, `no guess`, `빠짐없이/틀림없이/다름없이/더함없이`, `자산화`)을 철저히 준수하여 3가지 목적에 맞는 마크다운 파일을 생성했습니다.

고객의 요구사항 텍스트에 기재된 내용만을 명확하게 구조화하였으며, 합의되지 않은 임의의 추정이나 임의 기능 구현은 완벽히 배제했습니다.

### 📄 생성된 파일 목록 및 다운로드 안내

1. **개요 수준 (1장 이내, 요약본)**
   * 파일명: `10-기본-고객요구.md`
   * [file-tag: code-generated-file-2-1779876598580855384]

2. **전문가 구현 가능 수준 (기술/모듈 중심 설계)**
   * 파일명: `20-핵심-고객요구.md`
   * [file-tag: code-generated-file-1-1779876598580854304]

3. **비전문가 이해 가능 수준 (상세 시나리오 및 가이드)**
   * 파일명: `30-종합-고객요구.md`
   * [file-tag: code-generated-file-0-1779876598580848744]

---

### 📌 파일별 주요 수록 내용 요약

#### 1. `10-기본-고객요구.md` (개요 수준)
* 시스템의 핵심 목적인 '크롬 로그인 세션 기반의 3대 LLM 다중 라운드 토론 시스템' 정의
* R1(Blind 독립 응답), R2(교차 검토), R3(최종 답변)의 라운드 핵심 규칙 명시
* Chrome 전용 확장 프로그램(Manifest V3), 로컬 전용(서버 없음, API 미사용) 등의 핵심 기술 범위 한 장 요약

#### 2. `20-핵심-고객요구.md` (전문가 구현 수준)
* **모듈별 구조화:** `Session & Auth Manager`, `Round Control Engine (State Machine)`, `User Interaction Policy`, `Storage Subsystem` 등으로 분류
* **상세 제어 로직 수록:** 라운드 동기화(모든 응답 완료 후 다음 라운드 진입), 라운드 도중 1회씩 증감/마지막 회차 증설 제어 메커니즘, 1500 토큰 리스폰스 컨텍스트 제한 설계 검토 요구사항 명시
* **예외 처리 및 프롬프트 주입:** 타임아웃(기본 5분, 분 단위 설정), 재시도(3~9회), 프롬프트 내 메타데이터`[제한시간 + 현재차수/제한차수]` 전송 기술 요건 반영

#### 3. `30-종합-고객요구.md` (종합 안내 수준)
* **사용자 시나리오화:** 연결 탐색 실패 시 대응 워크플로우(크롬 로그인 유도 -> default 계정 생성 안내) 가이드
* **인터랙션 제약 사항 구체화:** 질문 후 응답 완료 전까지 입력창 차단, 라운드 대기 중에만 LLM 변경 및 1:1 재질의 가능 조건 설명
* **데이터 보존:** IndexedDB 라운드별 실시간 자동 저장, 날짜/LLM/라운드별 다중 조건 검색, 종합 및 개별 마크다운 파일 다운로드 기능 기술
* **개발 원칙 준수:** 명세서에 기재된 4대 개발 원칙 및 MVP 제외 항목(몰래 채팅 구현 금지)을 명확하게 명시하여 프로젝트 관계자 간 오해가 없도록 구성