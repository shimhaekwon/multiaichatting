# 226. P0 구현 계획 (ai-sarangbang)

> 자기완결. [221]~[225] 설계를 **P0(MockDriver · 솔로 · 메커닉 무결)** 코드로 옮기는 실행 계획. 구현 위치 `E:\workspace\ai-sarangbang`(빈 그린필드). **2회 설계 critic + 계획 critic 반영본** 기준.
> 변경 이력: v1 (초안) · v2 (계획 critic 반영 — whisper emit 결선·영속성 범위·i18n·store 슬라이스·D3 하드게이트·invariants·system line·테스트 레시피)
> 표기: 이모지 미사용. 충돌 시 규칙은 [221] §3 우선.

---

## 0. 착수 전 하드 게이트 [H4]
- **[확정 2026-06-02] D3 = 사람 우선 abort** — 사용자 확정 완료. 사람이 AI 출력 중 전송 시 현재 출력 abort(부분 보존·`stopped`) 후 새 턴. [222] §5 single-flight 설계 **그대로** → **S3 즉시 착수 가능**(completedNaturally 가드로 중단을 done 오분류 안 함).
- 이미 확정(직전 critic): D1=사람입력 턴시작 · D5=공개 done 전사(self 포함) · C-1=사람 status='done'. D2(P0=전원)·D4(좌석순)는 플래그로 흡수(코드 변경 작음).

## 1. P0 범위 · 전제
- **목표**: AI 없이(MockDriver) **floor 루프 · R1~R4 · 바지인 single-flight · whisper 휘발 · 90s UI** 완성·검증.
- **전제**: 솔로(사람 1 + AI N 목), 로컬 웹앱(프론트 only), 무료(외부 런타임 의존 0).
- **영속성 [C2]**: P0 = **in-memory 전용**. `persistRoom`/`loadRoom`·재수화 불변식([223] §5)은 **P0 옵션 토글 또는 P1**(기본 비활성) → `log/history.ts`는 P0 비대상([224] §4 history.ts도 P1로 격하). 단 in-memory `RoomSession` 구조 자체는 S1에 포함. (옵션 토글 켜면 S5에 추가.)

## 2. 스캐폴드 [L1 버전 핀]
- `npm create vite@latest . -- --template react-ts`.
- **버전 핀**: `react@^18.3` · `react-dom@^18.3` · `vite@^5` · `typescript@^5.4`(strict) · dev: `vitest@^1.6` · `@testing-library/react@^14` · `@testing-library/jest-dom` · `jsdom@^24` · `nanoid@^5`. (전부 무료, 외부 런타임 의존 0)
- `vitest.config.ts`(environment: jsdom).
- **eslint [H1/순수성]**: `no-restricted-imports`로 `src/core/**`에서 `react`·`react-dom`·DOM 차단 + **UI 한글 문자열 리터럴 0**(시연 데이터 제외) 규칙(또는 PR 체크).
- 폴더 = [224] §4 + i18n: `src/{core,drivers,ui,app,log,i18n}`.

## 3. 빌드 순서 (의존성 DAG — 하위 → 상위)

### S1. `core/types.ts` + `core/id.ts` + `drivers/AgentDriver.ts`(interface) [M3]
- 산출: [223] §1 타입 전부 · `newSessionId`/`newMessageId`(nanoid6) · **`AgentDriver` 인터페이스**(타입이라 여기 둠 → S3 스텁이 MockDriver 구현체 없이 의존, 역의존 차단) · **`SystemLine` 타입**(입퇴장·턴경계 — UI 전용, history/MD/스냅샷 미포함 [L4]).
- 완료: tsc strict 0.
- 테스트: id 포맷(`YYYYMMDD-HHmm-xxxxxx`).

### S2. `core/context.ts` ([223] §4) + `core/invariants.ts` [H2]
- 산출: `buildSpeakContext`(공개 done 전사·**self 포함**) · `whisperContext`(Whisper→SpeakContext 정규화) · `invariants.ts`(whisper 휘발 단언 · completedNaturally 가드 헬퍼, [224] §4·§5).
- 테스트(Vitest): **self 포함([C-2])** · `done`만 필터 · streaming/stopped 제외 · **whisperContext 정규화([M1])**(human→`__human__` · turnNo=-1 · status=done · 공개 history 0참조).

### S2.5. `app/store.ts` — UI↔core 동기화 [H3, S6 선결로 승격]
- 산출: 경량 외부 store(`useSyncExternalStore` + 자작 emitter, ~40줄). **구독 입자 확정**: `history`(append + 마지막 id in-place 갱신) · `floorHolder` · `turnState`(Map) · `whispers`(thread). coordinator의 `publish`/`onState`/`onWhisper`가 이 store를 갱신.
- 완료: tsc 0.
- 테스트: emit→snapshot 일관(같은 id in-place, append-only).

### S3. `core/coordinator.ts` ([222] §4 single-flight) — 핵심
- 산출: `Coordinator`(single-flight `runLoop`+`pending`+`busy`). 생성자 주입 `{room, driver, publish, onState, onWhisper}`([C1] whisper emit 채널 포함). 헬퍼 계약([222] §4.1): `beginMessage`/`appendToken`/`endMessage` = **publish 단일 소스**(UI는 id로 키해 in-place, Typewriter 자체 타이핑 금지).
- 드라이버 = **S1 인터페이스의 인라인 스텁 AsyncIterable**(테스트 파일 내 즉석 generator — S4 MockDriver 불요, M1 게이트 독립 [M3]).
- 완료: tsc 0.
- **테스트(critic must-verify)**:
  - R1 턴당 1회 · R2 floor 직렬 · R3 큐 소진→idle · abort 화자 turn-state=`stopped`([H-2]) · 드라이버 에러 Message=`error`·Participant=`stopped`([H1]) · `turnNo` 증가 · floorHolder null-윈도우 0.
  - **single-flight race [최우선·구체 레시피 M4]**: fake timer로 stream을 토큰 사이 멈춘 상태에서 `startTurn`을 **동기 연속 N회**(await 없이) 호출 → 마이크로태스크 flush → **`runTurn` 진입 카운터 max ≤ 1**(중첩 0) · `pending` 최신 1개만 · `turnNo` 증가 = 입력 수 · 각 abort Message·Participant=`stopped`. (진입 ++ / 종료 -- / max 단언으로 중첩 탐지.)
  - **whisper [C1]**: `onWhisper` emit이 토큰마다 발화·휘발(공개 history/MD 0오염) · **whisper-floor 독립**(턴 진행 중 whisper 가능, [M1]).

### S4. `drivers/MockDriver.ts` ([222] §4.1) + 하드타임아웃 래퍼
- 산출: `MockDriver`(스크립트 `{text; perTokenMs?; failAfter?}[]`, `signal.abort` 즉시 반응) · **하드 타임아웃 래퍼**(무종료 → stopped, [222] §6).
- 테스트: abort 즉시 중단·부분 텍스트 · `failAfter` 에러/무응답(타임아웃).

### S5. `log/md-export.ts` ([223] §3)
- 산출: `buildParticipantMarkdown`/`buildSummaryMarkdown`(결정적·공개만·whisper 제외) · Blob 다운로드 · `slug`. **다중 파일 [L3]**: 순차 호출 + 미세 지연(zip은 follow-up).
- 테스트: 결정적 출력 스냅샷 · whisper 미포함 · `status!=='done'` → `_(중단/응답없음)_`.

### S6. `ui/` ([225]) + `i18n/` [H1]
- 산출: `Room`/`MessageLine`/`Roster`/`Composer`/`WhisperPanel`(**`onWhisper` 구독** [C1])/`SaveBar`/`typing/Typewriter` + **i18n 사전(ko/en)** — 모든 UI·시스템 문구를 키로. 90s 토큰([225] §1) CSS.
- system line [L4]: `SystemLine`(S1) 기반 UI 전용 이벤트(입퇴장·턴경계) — history/MD 미포함.
- **완료기준 [M2 — 측정가능 분해]**:
  - (a) **자동(RTL 게이트)**: `[이름]` 라벨 렌더 · status별 클래스 · **바지인 시 *부분* 텍스트 + stopped 표식**(완료본 아님 [M5]) · whisper DOM 격리(공개 로그 미노출) · 이름 escape · 한글 리터럴 0.
  - (b) **수동 체크리스트(비-게이트)**: [225] §1.1 색 토큰 · §1.2 타이핑 28ms · 폰트 스택 적용 여부.

### S7. `app/main.tsx` + `config.ts`
- 산출: `createRoot` · **드라이버 주입 단일 지점**(P0=MockDriver) · store 연결.
- **config 기본값 [L2]**: `typingMs=28` · `hardTimeoutMs=5000`(P0 mock) · `WHISPER_TIMEOUT_MS=30000` · 단계 플래그.
- 완료: `npm run dev`로 동작하는 90s 사랑방(목 대화·바지인·귓속말).

## 4. 테스트 계획 — FR ↔ 테스트 매핑 [M1, 추적성]
FR-01→R1 · FR-02→R2 · FR-04→R3 · FR-05→whisper휘발+onWhisper · FR-08→single-flight race(레시피 S3) · FR-10→context self포함 · FR-12→abort guard · FR-14→md-export. + whisperContext 정규화 · whisper-floor 독립 · (영속성 옵션 시) loadRoom 재수화(streaming→stopped·status=idle·floorHolder=null·whisper 부재).

## 5. P0 완료기준 (DoD)
- [221] R1~R4 Vitest green + **single-flight race 0**(구체 레시피 헤드리스 실증).
- whisper 휘발(공개 로그·MD·스냅샷 미포함) + `onWhisper` 렌더 green.
- 90s UI 자동 게이트(S6 a) green.
- MD 저장(공개만) 동작.
- tsc strict 0 · `src/core` React/DOM import 0(lint) · **UI 한글 리터럴 0** · 회귀 0.

## 6. 마일스톤 (각 끝 Vitest green 게이트)
| M | 슬라이스 | 산출 |
|---|---------|------|
| M1 | S1~S3 + S2.5 | **메커닉 무결**(core + store + coordinator, 헤드리스 — UI 전) |
| M2 | S4~S5 | 드라이버(Mock) + 로그(MD) |
| M3 | S6~S7 | **동작하는 90s 사랑방**(UI + i18n + 와이어링) |

> **구현 상태 (2026-06-02) @ `E:\workspace\ai-sarangbang`**: **M1·M2·M3 green (P0 완료)** — 65 Vitest 통과(14파일) · tsc strict 0 · 프로덕션 빌드 green · `npm run dev` 동작 · core 순수성 + UI 한글 리터럴 0 게이트. single-flight race·abort·whisper 휘발·MockDriver·MD export·90s UI(터미널·[이름]·타이핑·roster·귓속말 오버레이·MD저장)·i18n(ko/en)·Room 엔드투엔드 실증. uw-critic 1차(coordinator) C1(willSpeak throw wedge)·H1(abort 무시 드라이버 stopped)·M2(whisper 대상 검증)·L1 수정+회귀. D3=사람 우선 abort 확정. 독립 git repo(`08b7be8` M1·M2 · `9bb7aa9` M3). **+ P1(OllamaDriver) 코드 완료(`cf190f6`)** — /api/chat NDJSON 스트리밍·signal abort·persona/roster→messages 라벨링, vite proxy(/ollama→11434 CORS우회), `config.driver` 'mock'|'ollama' 플래그(기본 mock). **coordinator.ts·src/ui diff 0 = 드라이버 교체만 입증**(AgentDriver 추상화 검증). SpeakContext에 roster 1필드 추가(라벨링·seam 자기완결). 76 Vitest green. **실 구동 = 사용자 Ollama 설치+`ollama pull <model>`+`config.driver='ollama'`. 다음 = P1 라이브(프롬프트 튜닝·모델 선택 O2) 또는 P2(API·유료 최후).**

## 7. 리스크 · 선결
- **D3 미확정 = S3 하드 게이트**(§0). 사람 인터럽트 시맨틱이 single-flight 토대.
- single-flight는 **설계 trace만** 됨 → S3 race 레시피(M4)가 진짜 게이트(설계 ≠ 실증).
- 영속성/재수화는 P0 옵션/P1([C2]). 둥근모·갈무리 폰트(U1)는 P0 후반.

## 8. 미해결
| ID | 항목 | 상태 |
|----|------|------|
| P-1 | store 자작 vs `useSyncExternalStore` 세부 | [미정] — S2.5 확정 |
| P-2 | MockDriver 데모 시나리오 자료 | [미정] — S4 |
| P-3 | i18n 키 전수 목록(입퇴장·상태·중단·휘발 배너 등) | [미정] — S6 |
