# 20260604-144124 처리내역 — ai-sarangbang 228(진입 로비·런타임 모델 선택) 구현·검증·커밋

## 세션 목적
"다음 업무 진행" 요청 → 메모리상 확정된 다음 작업 = **설계 변경안 228(진입 로비·런타임 모델 선택) 구현**.
(폐쇄망 셋업은 장비 확정 대기로 보류, qwen3.5:4b 미커밋 config는 본 구현 커밋에 합류.)

## 진단 (착수 전 실제 상태 검증 — 메모리는 과거 스냅샷)
- ai-sarangbang(`E:\workspace\ai-sarangbang`, 독립 repo): `src/app/config.ts`만 미커밋(qwen3:8b→qwen3.5:4b, 메모리 일치). main · origin/main 존재.
- 228 설계 문서(`220.설계산출물-ai-sarangbang/228-…`): v1.0, §7 9단계 + uw-critic C1/C2/H1~3/M1~4 §9.2 반영 확정.
- dev 서버: 이전 세션 잔재(5173~5175 점유), 내 background dev = **5176**.
- Ollama: 실행 중(curl 초기 `{"models":[]}`였으나 로비 로드 시 9개 정상 표시 — 일시 타이밍).

## 처방 (구현 — 설계 §7 9단계, 의존성 순서대로 직접 구현)
> verbatim 이식 요구 + 정확성 중요 → 에이전트 위임 대신 직접 구현.
1. **core/coordinator.ts**: `dispose()` + `disposed` 가드(runLoop while·runTurn for·startTurn/startAutoMode/whisper 진입 차단) + `activeWhispers` 추적(일괄 abort) — 전환·재진입 orphaned stream/타이머 0 (C2/M4)
2. **app/config.ts**: `RoomConfig`/`AiSlot` 타입, `defaultNameForModel`(org 제거→태그→첫 글자 Title), `guessThinking`(패턴); **core/id.ts** `newId`
3. **drivers/ollamaApi.ts**(신규): `listModels`(GET /api/tags, details 부재 옵셔널 가드 L3)
4. **app/persist.ts**(신규): save/load/clear(버전 게이트·try/catch·슬롯 구조 검증 H3) + `reconcileModels`(사라진 모델 R1)
5. **app/buildSession.ts**(신규): `buildSession`(ollama, 구 main.tsx 조립 verbatim=strip+hardTimeout+4 opts+모델/think resolver H1) + `buildDemoSession`(고정 demo id H2); **app/App.tsx**(신규, StrictMode-safe=세션 클릭에서만 생성·전환마다 이전 dispose); **app/main.tsx** 슬림화
6. **ui/Lobby.tsx**(신규): 목록·슬롯·인원 가감·tri-state think·AI≥1 게이트·미실행/0개 폴백, 전 문자열 i18n(L1)
7. **ui/Room.tsx**: `onLeave` + [⚙ 설정] 버튼; **vite.config.ts** `preview.proxy`(C1·D-8); **i18n/index.ts**·**ui/theme.css** 로비 문구/스타일
8. 테스트: coordinator(+dispose 4)·ollamaApi·persist·buildSession·App·Lobby

## 검증
- **게이트**: `tsc --noEmit` 0 · **vitest 128/128**(21파일, +31) · `vite build` 0. ui-no-korean(Lobby 한글 리터럴 0)·core-purity(Math.random 미사용) 게이트 통과.
- **uw-critic**(독립, 코드 33회 탐색): **판정 SHIP**(CRIT/HIGH 0). MED2(App 전환 배선 테스트 0 커버리지·설계 §4.6 예시 EXAONE 불일치) + LOW3(missing 배너 stale·미dedup·think 복원 방어) **전량 FIX**.
- **라이브**(나라장터 크롬, localhost:5176): 로비(9모델 listModels+parameterSize·이름 자동 Qwen35-4b-fast·사고 토글) → 인원 추가(슬롯 2) → 입장 → **ollama 직렬 발화**(qwen35-4b-fast ×2, roster 둘 다 완료) → [⚙ 설정] → 로비 복귀(**영속 슬롯 2개 복원**). dispose 전환 정상, freeze/누수 없음.
- **관찰사항(228 범위 밖)**: 둘째 AI가 `[나]`를 흉내내 사람 발화를 지어냄 → 동일 소형모델(qwen 4.7B) 2개 역할 혼동(프롬프트/소형모델 품질). 로비에서 **다른 모델 2개 선택 시 개성 분리**로 개선.

## 변경 파일 (20)
- 변경 10: config.ts, coordinator.ts, main.tsx, id.ts, i18n/index.ts, Room.tsx, theme.css, vite.config.ts, coordinator.test.ts, Room.test.tsx
- 신규 10: App.tsx, Lobby.tsx, buildSession.ts, persist.ts, ollamaApi.ts + 각 `.test`
- 커밋: **ai-sarangbang `8d5d6ca`**(push → origin/main, `2b43e70..8d5d6ca`) / **multiaichatting 228 문서 §4.6 `d851a5b`**

## 다음 단계
- 228 후속(D-1): 대화 중 모델 교체(MVP는 입장 고정 · [설정] 재구성).
- multiaichatting 정본 `221`§R2 · `222` 본문 갱신 미반영(이전 세션 미완, 이번 미관여) — 별도 진행.
- 폐쇄망 에이전트 셋업: 장비 확정 대기(개인 PC 반입 / 회사 GPU / Strix Halo 구매 미정, 용도=분석·설계·문서 한국어).
- dev 서버 5176 background 유지 중(정리 필요 시 종료).
