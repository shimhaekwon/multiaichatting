# 처리내역 — ai-sarangbang floor 랜덤직렬 구현(227) + qwen3.5:4b 교체 + 로비설계(228) + 폐쇄망/하드웨어/학습 상담

- 일시: 2026-06-04 13:39
- 대상: 소스 `E:\workspace\ai-sarangbang` (github `shimhaekwon/ai-sarangbang`, private) / 설계 `E:\프로젝트\multiaichatting\220.설계산출물-ai-sarangbang`
- 세션 성격: 라이브 디버깅 → 설계변경(227) 구현·검증 → 모델 교체·상담 → 후속 설계(228)

---

## 1. 세션 목적

사랑방(ai-sarangbang) 라이브 대화에서 **EXAONE·Qwen3가 연속 "응답 없음"** 으로 죽는 문제의 원인 파악 → 해결. 이후 모델 교체·폐쇄망 로컬 LLM 작업 방향 상담 + 후속 기능(진입 로비) 설계까지.

## 2. 진단 (응답 없음 원인)

- 증상: 대화가 길어지면 EXAONE·Qwen3 연속 "응답 없음", Phi4-mini만 발언.
- 실측 근거:
  - `ollama ps`: 전 모델 **100% CPU**(Intel Arc 130T 내장 GPU = Ollama 가속 불가) · `qwen3 Stopping`(언로드).
  - RAM 31.4GB / **free 7.6GB** < 3모델 워킹셋 14GB(5.1+3.0+5.9) → 동시 상주 불가.
  - 단독 추론 측정(`/api/generate`): phi4 39.4 / exaone 21.3 / qwen3 19.2 tok·s⁻¹(prompt-eval), 총 3~4s → **셋 다 단독은 정상**.
  - 타임아웃 배선 정상(ollama idle 60s, mock 5s 아님).
- 결론: **느림 자체가 아니라 "동시 race"가 저사양 자원(CPU·RAM)을 분할·경쟁**시켜 ① 작은 phi4 첫토큰 독식(망가진 결정성) ② 큰 모델 굶어 60초 idle 타임아웃 → `indicateNoResponse`. "물리적 동시 추론"이 단일 CPU·저RAM에서 자멸.

## 3. 처방

### 3-1. 설계변경 227 (race → 랜덤 순서 직렬)
- 철학 재정의: "진정한 대화감 = 순서 비결정·반응 개성·끼어듦·맥락 반응"이며 **물리적 동시 추론은 그중 어디에도 없음**. 동시성 포기해도 라이브함 보존 가능. (사용자 반론 "직렬화는 진정한 대화 아니지 않나"에서 출발 → "좌석순 직렬은 죽은 대화지만 랜덤순서 직렬은 다름" 정립)
- 동작: 사람 입력 → 발언 후보 **Fisher-Yates 셔플(rng 주입=결정성)** → 추첨 순서대로 **1명씩 직렬** 발언(자원 독점 → 저사양 안정). **직렬 보너스**: 뒤 화자가 앞 발언을 history로 봄(대화 연쇄). **순번 배지**(onOrder)로 추첨 순서 표시.
- uw-critic FIX-FIRST 반영 v1.0: **C1**(onOrder 배선 부재) **C2**(배지 생명주기/클리어) **H1**(streamMessage≠streamWinner floor연출) **H2**("응답없음" 테스트 직렬형 보존) **H3**(core-purity가 Math.random 미금지 → 호출 가드 추가) M1~4·L1~3 전량.

### 3-2. 227 구현
- `coordinator.ts`: race 4종(`raceRound`/`pullFirstToken`/`streamWinner`/`indicateNoResponse`) 삭제 → `shuffle()`/`speakOne()`/`onOrder` emit/직렬 `runTurn` + 미발언 queued 해제 + 생성자 `rng`(기본 Math.random 참조).
- `store.ts`: `RoomView.turnOrder` + `onOrder`. `Roster.tsx`: 순번 배지. `theme.css`: `.rank`. `core-purity.test.ts`: `/Math\.random\s*\(/` 호출 가드(참조는 허용).
- `coordinator.test.ts` 직렬 재작성(시드 rng 순열·직렬 무응답 보존·onOrder emit/clear). 테스트 헬퍼 turnOrder 추가.
- 검증: **vitest 97/97 green(16파일) · tsc 0 · vite build OK**.

### 3-3. qwen3.5:4b 교체
- `config.ts` `byParticipant.qwen3.model`: `qwen3:8b` → `qwen3.5:4b` (1줄, `think:false` 유지, 봇 이름 유지).
- ⚠️ pull(3.4GB)을 **사전 확인 없이** 실행 → 사용자 지적("다운로드는 물어보고"). **이후 다운로드/설치/대용량 작업은 실행 전 확인 원칙**.

### 3-4. 상담 (모델·하드웨어·학습·폐쇄망)
- **모델 변경 = config.ts 1곳**(+ pull·정확 태그·thinking이면 think:false). 봇 이름은 `demoParticipants`.
- **qwen3.5**: 8b 없음, 9b가 그 자리(MoE 30B-A3B ~100tok/s). 최신 한국어: **EXAONE 4.0**(32B/1.2B 양극단, ollama는 `ingu627/exaone4.0` 커뮤니티) · Qwen3.5 · EEVE-Korean 10.8B · DNA 8B.
- **PC 비교**: 이 PC(Intel Arc, CPU, 가속X) ≪ 개인 RTX4060 8GB(CUDA, 7~14B, QLoRA 가능) ≪ **Strix Halo(Ryzen AI Max+ 395) 64GB 통합메모리** = ASUS ROG Flow Z13(374만): iGPU Radeon 8060S, 통합 64GB로 **32B 쾌적·70B 가능**(dense 32B ~10-15 / MoE 30B ~100 tok/s), Vulkan 백엔드 권장, **분석/설계/문서·한국어 용도 최적**.
- **학습**: 흔히 말하는 "LLM 학습" = **가중치 갱신(fine-tune)** 이 맞음(사용자 정정 수용). RAG는 학습 아님(외부 기억). fine-tune은 NVIDIA/클라우드 QLoRA, 이 PC(Intel Arc) 부적합. "대화 기억" 목적이면 RAG가 실용적.
- **폐쇄망 로컬 에이전트**: Claude 작업방식 = 두뇌(LLM)+손발(도구)+절차(루프). 폐쇄망 재현 = 두뇌만 로컬모델 교체. 사용자는 이미 **OpenCode** 사용 → 절반 확보. 남은 건 로컬모델·GPU·RAG·검증자동화·오프라인 반입.

### 3-5. 설계변경 228 (진입 로비 — 런타임 모델 선택)
- 진입 시 로비에서 **`/api/tags`(CLI 아님) 모델 목록** → 슬롯별 모델·이름·사고모드(tri-state)·**인원 가감** → `buildSession` → 방. **localStorage 영속**. 사용자 희망 1~5 전부 반영.
- uw-critic FIX-FIRST 반영 v1.0: **C1**(`/ollama` proxy dev 전용 → preview.proxy/범위명시) **C2**(`Coordinator.dispose()` 부재 + StrictMode 이중·재진입 누수) **H1**(buildSession을 main.tsx 조립 verbatim) **H2**(mock 폴백 id 불일치 → 빈 방 → 고정 demo id) **H3**(영속 스키마 버전·try/catch·모델 교집합 검증) **M3**(think tri-state) M1~4·L1~3.

## 4. 검증

- 자동: vitest 97/97 · tsc 0 · vite build(52 modules).
- **라이브(브라우저, 나라장터 크롬, localhost:5175, 2턴)**: 응답없음 소멸(3모델 전원 발언) · 순번 ①②③ · 순서 랜덤(1턴 E·P·Q → 2턴 E·Q·P) · 턴 종료 배지 클리어 · **직렬 보너스 실증**(Phi4가 "[EXAONE] 날씨도 우수해 보인다니…" 앞 발언 인용·반응) · 10초/3모델(체감 양호).

## 5. 변경 파일 / 커밋

- **소스 `ai-sarangbang`**(github private 신규 — gh 부재로 API로 repo 생성):
  - `core/coordinator.ts`·`app/store.ts`·`ui/Roster.tsx`·`ui/theme.css`·`core/core-purity.test.ts`·`core/coordinator.test.ts`·`ui/Roster.test.tsx`·`ui/WhisperPanel.test.tsx` (227)
  - `README.md` 신규(요구/설계/구현)
  - **미커밋**: `app/config.ts`(qwen3.5:4b) — 다음 세션 커밋 예정
  - 커밋: `8ce01f5`(feat floor 직렬) → main 머지 → `2b43e70`(README). origin/main push.
- **설계 `multiaichatting`**(github):
  - `220.설계산출물-ai-sarangbang/227-…md`(v1.0) · `228-…md`(v1.0) · `CHANGELOG.md` 신설 · `README.md`(버전이력 3층 체계: 문서메타+README표+CHANGELOG)
  - 커밋: `237459f`(227+체계) · `1d6c67d`(228). origin/main push.

## 6. 다음 단계

1. **228 구현**(다음 세션): `Coordinator.dispose()` → `listModels` → `buildSession`/`App`(StrictMode-safe) → `Lobby` → `persist` → 테스트 + 라이브(dev & preview). `vite.config` preview.proxy.
2. ai-sarangbang `config.ts` qwen3.5:4b **미커밋** → 다음 커밋에 포함.
3. **폐쇄망 로컬 에이전트 셋업(1번)**: 장비 확정(개인 PC 반입 가능 여부 / 회사 GPU / Strix Halo 구매 등) 대기 → 확정 후 모델·반입 체크리스트 구체화. 주 용도 = 분석/설계/문서(한국어).
4. (참고) 진짜 fine-tune은 NVIDIA/클라우드 QLoRA, 이 PC 부적합.

## 7. 잔여/주의

- dev 서버(localhost:5175) background 실행 중(사용자 "열어놓았음" 요청 → 유지). 필요 시 정리.
- 227은 [221]§R2(속도경쟁→랜덤직렬)·[222](floor 흐름·speakOne·onOrder) **정본 본문 갱신 미반영**(README 각주·CHANGELOG에 "개정 예정" 표기) — 후속.
- 다운로드/설치는 사전 확인 원칙(이번 pull 무단 실행 교훈).
