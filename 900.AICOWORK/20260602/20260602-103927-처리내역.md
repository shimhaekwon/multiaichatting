# 처리내역 — 2026-06-02 10:39 (Task #7 네트워크 인터셉트 Phase 1 착수·커밋)

> 대상: `E:\workspace\multichat` (ChatHub 개조 MV3). 전 세션("이어서 진행") 다음단계 중 ③네트워크 인터셉트 트랙.
> 사용자 요청="이어서 진행" → 트랙 선택 시 "추천안 요청" → **네트워크 인터셉트 Phase 1 추천·착수**.

## 세션 목적
- 직전 세션 다음단계 ①사용자 통합테스트(미수신·확장리로드 필요) ②하이브리드 루프 ③네트워크 인터셉트 ④DeepSeek staleness 중 방향 선택.
- 추천: **③ 네트워크 인터셉트** — 남은 최대 미해결(백그라운드 throttle)의 유일 근본해결책. 현 안전망(60s/180s/포그라운드 힌트)은 "영구 hang"만 막았고 throttle 자체는 미해결(탭 포그라운드 강제 잔존).

## 결정 (주입 아키텍처)
- 백그라운드 throttle 근본원인 = hidden 탭 rAF 정지 → ChatGPT/Gemini 응답이 DOM 미렌더 → DOM 폴링 done 미발화.
- 해결 = **MAIN world 네트워크 인터셉트**(fetch/SSE 캡처, 렌더·가시성 무관).
- 주입 방식 = **`chrome.scripting.executeScript({ world:'MAIN', func })`** 로 확정. 후보 비교:
  - crxjs `content_scripts`+world:MAIN ✗ (crxjs beta.21 world 미지원 + async ESM 로더가 MAIN world서 chrome.runtime 부재로 불가)
  - 인라인 `<script>` ✗ (ChatGPT/Claude/Gemini CSP 차단)
  - **executeScript({world:MAIN, func}) ✅** (CSP 면제, 2차빌드/WAR 불요, vite target chrome87+라 async 네이티브 → func.toString() 직렬화 안전)
- 타이밍: 완성 fetch는 전송 시점 발생 → SEND_PROMPT 처리 중(전송 직전) 주입이면 항상 선행 패치. document_start 불요(필요시 Phase 3 승격).

## 구현 (Phase 1 = 진단/학습, 순수 추가형)
- **신규** `src/content-script/net-intercept.ts` — `mcNetInterceptor()` 자기완결 MAIN-world IIFE. `window.fetch`+`XMLHttpRequest.prototype` 패치, `looksLikeCompletion` 휴리스틱 매치 시 응답을 **clone()만** 읽어(페이지 응답 무변형) `window.__mcNet` 링버퍼(40엔트리/200KB 상한)에 적재. `window.__mcNetInstalled` idempotent 가드. 외부참조 0(표준 web 전역만) → 직렬화 안전.
- **수정** `src/background/index.ts` — `installNetInterceptor(tabId)`=`executeScript({world:'MAIN',func})`. SEND_PROMPT 의 `ensureTab` 직후·CS 연결 전 `await` 호출.
- **무변경**: `dom-adapter.ts`·`manifest`·content_scripts·done 판정. (Phase 1은 캡처만, 라운드 로직 미사용)

## 안전 불변식
1. 페이지 응답 무변형(원본 그대로 반환, clone만 읽음). fetch 원본 먼저 호출, 캡처는 try/catch best-effort.
2. idempotent 가드(매 전송 호출해도 이중 패치 없음).
3. 캡처는 링버퍼에만. done/라운드 미관여 → 깨질 표면 없음.
4. 롤백 = `installNetInterceptor` 호출 1줄(또는 import) 제거 → 기존 동작 100% 복귀.

## 검증 (정적 — 가능한 전부 green)
- `tsc --noEmit` 0 (world:'MAIN' + imported func 타입 통과)
- `vitest` 19/19 (회귀 없음)
- `yarn build` green / background 번들 `executeScript({world:"MAIN",func:v})` + `v` 자기완결(esbuild 헬퍼 참조 0) 확인.
- **런타임 = MCP 불가**(chrome-extension·MAIN world 접근불가) → 사용자 확장 리로드 검증 필요.

## 변경/커밋 (multichat)
- commit `b95ad52` "Task #7 네트워크 인터셉트 Phase 1 — MAIN world fetch/XHR 캡처(진단 전용)" (2 files, +278). origin 미연결(로컬 master).
- 산출 문서: `E:\프로젝트\multiaichatting\210.설계산출물-구현적용\224-네트워크인터셉트-task7.md`(아키텍처 결정·Phase 1/2/3·사이트별 캡처표[TBD]·검증절차·리스크).

## 사용자 검증 절차 (Phase 1 — Phase 2 진입 게이트)
1. `E:\workspace\multichat` 이미 빌드됨 → `chrome://extensions` ↻ 리로드.
2. app.html → ChatGPT·DeepSeek 등 2~3종 선택 → **1라운드 전송**(응답 끝까지).
3. 각 LLM **사이트 탭** 콘솔(F12)에 `window.__mcNet.dump()` → 출력(url/ctype/kind/head/tail/req) 복사 보고.
   - 안 보이면 `window.__mcNetInstalled` 확인(undefined=주입실패→보고).

## 다음 단계
1. **Phase 2(파서·done 결선)** — 사용자 캡처 데이터 수신 후 착수: 사이트별 SSE 텍스트추출기(순수함수+vitest 픽스처) → MAIN→ISOLATED postMessage 청크 스트리밍 → dom-adapter가 네트워크 캡처를 **1순위 done/텍스트 소스**로(DOM은 fallback). throttle 완전해결 + **DeepSeek #4 staleness 자연해소**.
2. **#4 DeepSeek staleness = 의도적 보류** — observeResponse 코어 수정은 안전망(`!lastText && hidden`)과 상호작용해 blind 수정 위험 + Phase 2 네트워크 done이 이 버그를 무력화(subsume)하므로 단독 패치 비효율.
3. Perplexity가 fetch 아닌 socket.io/WebSocket이면 Phase 2에서 `WebSocket` 생성자 패치 추가.
4. (선택) Phase 3 = registerContentScripts document_start 승격(전체 fetch 커버) — 독립 IIFE 빌드 필요, 현 방식 충분하면 보류.
