# 처리내역 — 2026-06-02 12:46 (ai-sarangbang 요구사항·설계산출물 — ultrawork)

> 대상: 신제품 VER-003 `ai-sarangbang`(AI+사람 라이브 턴제 채팅방, 90s PC통신 감성). v1(multichat) 아카이브 후 신규.
> 모드: ultrawork(업무처리원칙 v4 준수). 구현 전 단계 — 요구사항 초안 + 설계산출물 작성.

## Goal (why)
무료·솔로·웹앱 제약 하에 "AI+사람 라이브 턴제 채팅방"을 **구현 전에 규칙·코디네이터·아키텍처를 무결하게 확정**하기 위한 요구사항 초안 + 설계산출물 작성. (v1 웹스크래핑 fragility를 접고 AgentDriver 추상화로 mock→Ollama→API 단계 전환.)

## What changed (in-scope, 10개 신규 문서)
요구사항 초안 — `100.START/VER-003-ai-sarangbang/` (VER-001 3단계 포맷 미러링):
- `user-request.md`(원시 비전·4규칙 원문) · `10-기본.md` · `20-핵심.md`(FR-01~16) · `30-종합.md`

설계산출물 — `220.설계산출물-ai-sarangbang/` (210 하우스 스타일, 이모지 미사용):
- `221-제품개요-및-핵심규칙.md` — 제품 정의·범위(P0/P1/P2)·**4규칙 R1~R4 정밀 정의**·모호함 해소(D1~D5 제안)·코딩지침
- `222-coordinator-상태머신.md` — **심장**. 단일 권위 floor 루프(race-free)·바지인 interrupt·whisper·AgentDriver seam
- `223-데이터모델-로그포맷.md` — TS 타입(Participant/Message/Intent/RoomSession/Whisper)·MD 포맷·whisper 휘발 불변식·영속성
- `224-아키텍처-스택-단계.md` — 3레이어(UI/Coordinator/Driver)·Vite+React+TS·P0/P1/P2 완료기준·carry-forward 매핑·멀티유저 이식
- `225-UI-UX-설계.html` — 90s PC통신 감성 UI/UX(디자인 토큰·레이아웃·컴포넌트 매핑·turn-state 시각화·귓속말·접근성) + **인터랙티브 목업**(자기완결 HTML, floor 타이핑·바지인 중단 데모). [221] O1 확정.
- `README.md` — 폴더 인덱스(225 포함)

핵심 설계 결정:
- **Coordinator = 단일 소비자 floor 루프**(공유 boolean 제거 → Gemini의 TOCTOU 레이스 회피). 참가자는 intent enqueue만, 출력 권한은 루프만 부여.
- **AgentDriver 추상화**(`speak(ctx,signal):AsyncIterable<token>`) = 백엔드 단일 seam. mock/Ollama/API 교체 시 Coordinator/UI 무변경.
- **바지인=floor 선점/대기**(AI 간 중간 끊기 아님). 사람만 하드 인터럽트(abort, 부분 보존).
- v1 carry-forward: 비밀채팅→**귓속말(휘발)**, context빌더 self-exclusion, MD 결정적 생성, completedNaturally 가드, done 안전망. DOM/chrome/throttle 제외.

## Verified (실행 + 관찰)
- **uw-explorer ×2**: 문서 컨벤션(100.START 3단계·210 하우스스타일·이모지 회피) + v1 carry-forward(214/216/222) 실측.
- **uw-implementer ×3**: 파생 7문서 병렬 작성(221/222 정본을 읽어 일관성).
- **uw-critic 적대 검수**: CRITICAL 3·HIGH 4·MEDIUM 5·LOW 4 발견.
- **must-fix 전량 수정 + grep 실증**:
  - C1(바지인 race): 222 §4 `interrupt()`+`epoch` 가드 재작성 → 단일 루프 보장. **직접 trace로 race 해소 확인**(스트림 resolve 후 `myEpoch!==epoch` 가드, 로컬 ac).
  - C2/C3/L1(223 "작성예정" 오기): README·224 정정 → grep `작성 예정|미작성|타입-MD포맷` = 0.
  - H1(MessageStatus "1:1" 거짓·에러 분기): 문구 정정 + drainFloor가 `done/stopped/error` status 반환.
  - H2(whisper Map·타임아웃): 222 §4에 `whispers:Map`+`whisper()`+`WHISPER_TIMEOUT_MS` 명시.
  - H3(`[나]` 하드코딩=i18n 위반): MD 템플릿 `[{human.name}]`로, 정합 문구 정정.
  - H4(D4 좌석순인데 seat 부재): 223 `Participant.seat` 복원.
  - M3(turnNo 증가 누락)·L2(MD 경로 `md-export.ts` 통일)·L3(`RoomSession.floorHolder`) 반영.
  - grep 222: interrupt/epoch/floorHolder/seat 20건 확인.

## Residual risk + follow-ups (critic 미수정분 — 사용자 판단/구현 시)
- **[D1~D5 미확정]** §4 모호함 5건은 **제안값으로 작성**(no guess: 추측 아닌 명시 제안). 사용자 확정 필요. 특히 **D3(사람 하드 인터럽트=사람 우선 abort)** 가 바지인 UX 핵심.
- M2: `Message.role`이 `by.kind` 복제(이중 진실원) — 구현 시 단일 채움 함수 or 제거 결정.
- M4: `loadRoom` 재수화 시 coordinator 런타임상태(spokeThisTurn/queue) 복구 안 함 — 진행 중 턴 폐기·idle 복귀(의도). 명문화 권장.
- M5/C4: 전원 opt-out 연속 시 "죽은 방" — P0은 전원 발언 기본이라 무해, P1 willSpeak 훅 활성 시 안내 필요.
- L4: `doneDebounceMs=1500` "v1 계승" 좌표 약함(v1 실측은 컨텍스트별 상이).
- 미커밋: 9개 문서 git 미커밋(사용자 push 지시 시 `multiaichatting` origin 반영).

## 추가: 완전 재검토 2차 (사용자 "완전 재검토" 요청) + 수정
- **3렌즈 병렬 uw-critic**(정합성·추적성 / coordinator 정확성 / 완결성·UI) + 222 직접 정독.
- **발견(이전보다 심층)**: [CRIT] C-1 사람 메시지 status 미정의→AI가 질문 못 봄 / C-2 self-exclusion이 `m.by!==speakerId`로 자기발언 전체삭제→stateless 드라이버라 AI 치매(주석=직전, 코드=전부). [HIGH] H-1(coord) interrupt가 `await runTurn` 자기콜스택→연속 바지인 시 **runTurn 중첩**(epoch 가드 밖 head/tail 경합)=race-free 무효 / H-2 abort 시 drainFloor return이 Participant=stopped 스킵(speaking 고착) / H-3 whisperContext→SpeakContext 변환 미정의·done debounce 1500 DOM산물 오이식·헬퍼 계약 부재 / 225 컴포넌트(Roster 등) 224에 없음·error를 turn-state로 오분류·whisper 데모가 내용 공개로그 노출 / 223 import·slug 미정의 / README 잔여범위 stale.
- **수정(must-fix 전량)**: **222 v3 = single-flight 재설계**(epoch 폐기, `pending`슬롯+`busy`가드로 runTurn 중첩 원천차단, abort 화자 stopped 즉시확정, whisperContext, debounce 제거=하드타임아웃만(드라이버), 헬퍼 §4.1 계약, enqueue 가드, floorHolder null윈도우 제거). **223** self-exclusion→**공개 done 전사(self 포함)** 정정·human status='done'·whisperContext·import/slug·loadRoom floorHolder. **224** ui 컴포넌트 추가·done안전망 위치(driver)·FR-09충돌. **225** error 표기·whisper 데모 내용제거·dead code·escape·disclaimer. **20-핵심** FR-10/11·D5 정합. **README** C2~C5/A1~A4.
- **검증**: grep 잔여오기 0(구 필터·debounce·mdExport·dead code) + single-flight 직접 trace(stuck-pending race·연속 바지인 무중첩 확인).
- **잔여(surface, 사용자/구현 판단)**: M-B FR→설계 역추적표 미작성(권고) · L-B 225 AI-4 스와치·U4 "7+"문구 · L-C MD/UI status 표기차 주석 · seat 고유성 미강제 · A1 UI↔core 동기수단 미정 · single-flight는 **P0 Vitest 실증 권장**.

## 추가2: P0 구현계획(226) + 계획 critic + push
- **push**: ai-sarangbang 10문서 + 폴더 리네임(구현적용→multichat, VER-002→VER-002-multichat) + 처리내역 → `multiaichatting` origin commit `467aa70`.
- **226-구현계획-P0.md 작성**: 스캐폴드·의존성 DAG(S1 types/driver-interface → S2 context/invariants → S2.5 store → S3 coordinator → S4 MockDriver → S5 MD → S6 UI/i18n → S7 wiring)·슬라이스별 Vitest·DoD·마일스톤(M1 헤드리스무결/M2 드라이버·로그/M3 동작사랑방).
- **계획 critic 발견·수정**: [CRIT] **whisper 스트리밍 UI emit 경로가 222에 없음**(publish/onState=공개전용, whisper 둘다 못탐)=설계결함 → **222에 `onWhisper(target,thread)` 휘발 채널 추가**(토큰마다 emit, history/MD 무오염) / 영속성 P0 누락 → **in-memory 전용** 명시(persist/loadRoom=P1/옵션, C2). [HIGH] i18n(221§6)·`invariants.ts`(224§4) 누락 / store가 S6 선결인데 순환 / **D3 미확정인데 single-flight 토대** → **226 v2 재작성**(store **S2.5 승격**·**D3 §0 하드게이트**·i18n 슬라이스+DoD·SystemLine 타입·**single-flight race=fake-timer 중첩카운터 레시피**·버전핀·config 기본값). [MED/LOW] 시각일치→RTL게이트+수동체크리스트·MockDriver S3↔S4 역의존(interface S1로)·다중 Blob·system line 등.
- **이 세션 마지막 커밋 포함**: 226(신규)·222(onWhisper)·README(226 인덱스).

## 다음 단계 (resume)
1. **D3 확정**(사람 우선 abort[제안] vs 사람도 큐 대기) — 226 §0 **하드게이트**, single-flight 토대. (D1·D5·C-1 이미 확정, D2·D4 플래그 흡수)
2. D3 확정 후 **P0 착수**: 226 S1→S2→S2.5→S3 순, 각 Vitest green. **M1(헤드리스 메커닉 무결)이 첫 게이트**. single-flight race는 S3 fake-timer 중첩카운터 레시피로 실증.
3. P0 위치 = `E:\workspace\ai-sarangbang`(빈 그린필드). 설계 정본 = `220.설계산출물-ai-sarangbang/221~226`.

## 원칙 회고 (업무처리원칙 v4)
- ① 빠짐없이: 4규칙·whisper·턴흐름·MD·단계·carry-forward 전 영역 enumerate(FR-01~16). 
- ② 틀림없이: critic 검수로 타입·상태 정합 검증, grep 실증.
- ③ 더함없이: 요구사항 초안 + 설계산출물만(구현 코드 미작성 — 사용자가 "구현은 추후"). follow-up은 별도 섹션.
- ④ 다름없이: VER-001 3단계·210 하우스스타일·v1 carry-forward 패턴 일관.
- ⑤ step-by-step: 원칙로드→explorer→정본작성→implementer→critic→수정→검증→처리내역.
- ⑥ no guess: 미확정(D1~D5)은 추측 박지 않고 [제안]+[미정]으로 명시·표면화.
- ⑦ Challenge: Gemini 코디네이터 레이스 버그를 설계 채택 전 지적·정정(222 §2). critic 발견 전량 사용자에 보고.
