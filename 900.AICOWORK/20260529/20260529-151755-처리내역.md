# 처리내역 — ChatHub 기반 다자 LLM 브레인스토밍: 구현적용 설계산출물(210) 작성 + 자기완결화

- 일시: 2026-05-29 15:17
- 작업 위치(cwd): `E:\프로젝트\multiaichatting`
- 소스 코드베이스: `E:\workspace\multichat` (ChatHub v1.45.7, MV3 크롬확장)
- 성격: 코드베이스 분석 + 설계산출물(문서) 작성 (실코드 구현은 후속 별도)

---

## 1. 세션 목적
ChatHub(multichat)를 베이스로 "다자 Cowork 브레인스토밍 모드"(라운드 기반 다중 LLM)를 **누구나 추가 질문 없이 구현 가능**하도록, 추상설계(200)를 ChatHub 코드에 맞춘 **구현적용 설계산출물(210)** 작성.

## 2. 진단 / 주요 확인 사항
- **ChatHub 전체 분석**: 봇 추상화(`AbstractBot`/`AsyncAbstractBot`, 이벤트 스트림 계약), 18종 봇, Jotai `chatFamily`, `MultiBotChatPanel`(2×3 그리드·`onSwitchBot`·`sendAll`), proxyFetch CORS우회 → 산출물 `E:\workspace\multichat\000.AICOWORK\project-analysis-claude.md`.
- **버전 불일치(중요)**: 우리가 가진 오픈소스 = **1.45.7 = GitHub main 최신**(bots 폴더 동일, DeepSeek/Gemma 코드 없음). 구동 중 확장(DeepSeek·Gemma·MS Copilot)은 **비공개 상용 빌드** → 더 최신 오픈소스 입수 불가.
- **봇 접근 방식 근본 차이**: ChatHub 웹앱봇 = 사이트 내부 HTTP/SSE API(proxyFetch). 신규설계(원안) = 순수 DOM 조작 → ChatHub 봇 로직 대부분 재사용 불가.
- **요구사항 진화**: 원본 10종 → VER-002에서 6종 우선출시 확정, 토큰 combo·라운드 {3,5,7,9}·YAML·최소1 명시.

## 3. 처방 / 결정 사항
- **봇 전략 = 하이브리드(C)**: ChatGPT·Claude·Perplexity(내부 API) 재사용 + Gemini웹·DeepSeek·Grok웹(DOM) 신규.
- **프레임워크 재사용**(React+Vite+Jotai+Tailwind+사이드패널+그리드), **범위 밖 제거**(프리미엄·라이선스·분석·웹검색·불필요 봇 15폴더).
- **라운드 엔진 = 앱 컨텍스트** + `chrome.storage.session` 스냅샷(원안 SW엔진 부분수정). **교차전달 = `prompt + YAML(타 LLM 응답)` → 기존 `bot.sendMessage` 재사용**(self-exclusion).
- (참고) 초기 "vanilla 무빌드" 검토 → 품질·관리 기준으로 S3(TS+esbuild) 권장했으나, 사용자가 ChatHub framework 재사용으로 방향 전환 → S3 폐기.

## 4. 검증
- 클래스명·파일경로 실측 일치(`ChatGPTBot/ClaudeBot/PerplexityBot`·`ClaudeWebBot`·`PerplexityLabsBot`·`chatFamily`·`use-chat`·`ConversationPanel` props·`ChatMessageModel`).
- VER-002 ↔ 210 정합(토큰/라운드 콤보·6종·YAML·노출6) 반영 후 grep로 stale 값 0 확인.
- **구현 준비도 검토 → 갭 G1~G13 식별 후 전체 코드수준 보완** + **210 자기완결화**(200-초안 인용 중립화, grep 잔존 2건=라벨 provenance만) → "200 안 보고 구현 가능" 도달.
- ⚠️ 본질적 경험 항목 2건(신규3종 selector·재사용봇 노후)은 절차+fallback 문서화로 자가해결(Phase 0/2 실증).

## 5. 변경/생성 파일
| 경로 | 변경 |
|---|---|
| `E:\workspace\multichat\000.AICOWORK\project-analysis-claude.md` | 생성(ChatHub 전체 분석) |
| `E:\프로젝트\multiaichatting\100.START\user-request-ver.002.md` | 생성(ver.002 정형화 + 변경이력) |
| `E:\프로젝트\multiaichatting\210.설계산출물-구현적용\` | **폴더+10문서 생성**(README+211~219), ver.002 반영, G1~G13 보완, 자기완결화 수정 |
| `C:\Users\PC\.claude\plans\e-workspace-...harp.md` | plan 파일(구현적용 산출 계획) |

> ※ `200.설계산출물 → 200.설계산출물-초안` rename, `100.START`의 VER-001/VER-002 폴더화는 **사용자가 수행**. multichat 소스·musicmart 미변경.

## 6. 다음 단계
- **Phase 0 게이트**: `yarn build` + 재사용봇 3종(ChatGPT/Claude/Perplexity) **실동작 실증**(1.45.7 노후 가능) → 깨지면 DOM 전환.
- 신규 3종 selector PoC(DevTools 검증 → `adapter-config.json`).
- 210 기반 실제 코드 구현(Phase 1~8).
- (선택) 200-초안 ↔ 210 최종 대조 감사.

## 7. 6-6 원칙 회고
- ① 빠짐없이(FR-01~14·갭 G1~G13 전수) / ② 틀림없이(클래스명·경로 실측, grep 검증) / ③ 더함없이(범위 밖 제거, 문서수정도 OK 후) / ④ 다름없이(AbstractBot/Jotai 패턴 미러링) / ⑤ step by step / **⑥ no guess**(musicmart 오타·버전불일치·기준 코드베이스 매번 확인) / **⑦ Challenge**(무빌드↔TS 충돌, 휘발↔자동저장, ConversationPanel 재사용 불가, 봇 노후 리스크 사전 alert) — 전반 충실 적용.
