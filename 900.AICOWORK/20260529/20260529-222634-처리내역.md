# 처리내역 — 210 설계산출물 최종 검토 + 이슈 개선

- 일시: 2026-05-29 22:26
- 작업: `210.설계산출물-구현적용` 최종 검토 → 발견 이슈 설계문서에 직접 반영(개조)
- 모드: ultrawork (업무처리원칙 v4 + 코드생성원칙 적용)
- 베이스 대조: 요구사항 `100.START/VER-001·002`, 코드 `E:\workspace\multichat`(ChatHub v1.45.7)

## 1. 검토 방법 (실증 기반, 추정 배제)
- 요구 7파일 + 설계 12문서 전수 정독.
- 설계가 주장하는 베이스코드 계약을 **직접 파일 대조**: `abstract-bot.ts`, `use-chat.ts`, `state/index.ts`, `bots/index.ts`, `errors.ts`, `consts.ts`, `package.json`, `manifest.config.ts`, `tsconfig.json`, `MultiBotChatPanel.tsx`, `ConversationPanel.tsx`, `ChatMessageInput.tsx`, `ChatbotName.tsx`, `SwitchBotDropdown.tsx`, `background/index.ts`, `types/chat.ts`, `export.ts`, `chatgpt/index.ts`, `use-enabled-bots.ts`.
- 제거대상 모듈의 숨은 의존성 grep(서브에이전트) + 어드버서리얼 리뷰(uw-critic).

## 2. 검토 결론 (개선 전)
- 설계 인터페이스/파일경로/selector/계약 정확도 매우 높음(~8.5/10).
- 단, 문서 그대로 빌드 시 **무질문 perfect build ≈ 15~20%**: 런타임 무동작 로직버그 2건 + "재사용 수정없음" 오분류로 인한 컴파일 깨짐 + 6사이트 DOM 어댑터의 반복 실측 본질.

## 3. 반영한 개선 (설계문서 직접 수정)

### CRITICAL
- **C-1 (215)**: 엔진 응답 수집이 stale React 클로저(`s.chat.messages`)로 빈 값 → `useStore()`+`store.get(chatFamily(...)).messages`/`store.get(roundSessionAtom)`로 **최신 atom 읽기**로 교체. `setRoundSession(d=>…)` 일관화.
- **C-2 (218,214)**: 첫 전송 `start() && sendPrompt()` 단락평가+비동기 타이밍 버그 → `handleSubmit`(start-필요시 후 sendPrompt)로 교체. `sendPrompt`가 세션을 store에서 읽어 start 직후 동일 tick 안전. `maxRoundsAtom`/`tokenLimitAtom` 신설(214 §2), SettingsBar·핸들러·start 공유.

### MODERATE
- **M-1 (212,218 §8,219,220)**: "재사용 수정없음" 오분류 교정 → `ChatMessageInput`(prompts/PromptCombobox/PromptLibrary/plausible), `abstract-bot`(sentry→no-op), `History/Dialog`(use-premium), `Share/*`·`export.ts`(plausible)를 **개조**로 재분류 + side-effect grep 목록에 prompts/PromptLibrary/PromptCombobox/use-premium 등 추가. plausible 전수 호출처 9파일 명시.
- **M-2 (217,213)**: selector 단일 출처 모순 해소 → 봇 클래스는 `llm`만 보유, content script가 `adapter-config.json` 로드·조회. SEND_PROMPT 메시지에서 `selectors` 제거. ERR_001~005 전수 MAP. STOP 핸들러 scope 버그 수정. "무재빌드" 과장 완화.
- **M-3 (216,215,213,220)**: VER-002 14-1 리터럴 준수 — 1라운드에 타 LLM 키만 빈 값으로 전달(기존 "생략 권장" 변경). `buildContextForLlm`에 `participants` 인자 추가.
- **M-4 (216)**: `gpt3-tokenizer` 지연 init + CJS default-interop 주의 + 전체 try/catch 폴백.

### uw-critic 추가 지적 반영
- **H2 (216)**: `import yaml` → `import { stringify as yamlStringify } from 'yaml'`(esModuleInterop:false 회피).
- **H3 (217,220,221)**: gemini done 판정 모순 해소 — generatingFlag(div.thinking) 아닌 **stopButton 소멸**(221 §7 실측)로 통일.
- **M2(critic) (215)**: 중단 슬롯의 부분응답을 completed로 오분류 → "STOP 전 자연완료"만 completed, 부분/중단 폐기(VER-001 3.3).
- **M1(critic) (213 §5/§3)**: `bot.sendMessage({prompt,signal})` 흐름을 실제 `useChat.sendMessage(string)` 경로로 정정.
- **L1 (218 §7)**: 저장은 chrome.downloads로 통일(214 [G6]).

### MINOR
- swap 중복 botId 차단 + WAITING 중 차단(215/218). stopped 판정 정정. deepseek 로고·types/round.ts 신규파일 체크리스트 추가(212 §9). claude/gemini/grok/perplexity stop selector 영어/아이콘 fallback(221 §7).

## 4. 수정 파일 목록 (210)
211, 212, 213, 214, 215, 216, 217, 218, 219, 220, 221, README — 총 12개 중 12개 터치(대부분 소·정밀 수정).

## 5. 개선 후 평가
- 무질문 perfect build 확률: 약 15~20% → **약 45~60%**로 상승 추정(C-1/C-2/M-2 로직버그 제거 + 컴파일 깨짐 사전 차단). 잔여 변수: 6사이트 DOM 어댑터의 실환경 반복 튜닝(contenteditable 주입·done debounce·미로그인), 사이트 selector 노후. 이는 코드 미작성(설계 단계)의 본질적 한계로 실증(Phase 2 코딩+왕복)에서만 해소 가능.

## 6. 잔여(의도적 비변경 — critic도 비치명 판정)
- L2 createBotInstance 반환타입 DomAdapterBot(추상) — concrete 서브클래스로 충족, OK.
- L3 perplexity `:has(use[*|href$=…])` — 221에 closest() 폴백 명시됨.
- M3(padTo6 더미 alias) — slots 제외로 무해, 문서화됨.
- 211 §6 경로 alias 표현(단일 `~*` 글로브로 전부 해소 — 기능상 정상).

## 7. 6-6 원칙 회고
- ① 빠짐없이: FR-01~14 + 베이스 계약 전수 대조, 숨은 의존성까지 enumerate.
- ② 틀림없이: 모든 지적/수정을 실제 코드 줄번호로 검증.
- ③ 더함없이: 검토·문서개조 범위만. 신규 기능 0(세션복구·swap는 기존 문서화된 항목).
- ④ 다름없이: AbstractBot/Jotai/ConversationPanel 등 기존 패턴 기준 정합.
- ⑤ step by step: 요구→설계→코드대조→교차분석→수정→critic 검증.
- ⑥ no guess: M-3는 사용자 명시 요구(14-1) 리터럴 준수로 해소(추정 아님). selector는 실측값 기준.
- ⑦ challenge: "perfect build 가능" 낙관 전제에 로직버그 근거로 위험 선제 보고 후 개선.

## 8. 후속 권고
- (구현 시) Phase 2: dom-adapter.ts/DomAdapterBot 코딩 + 6종 단발 왕복 실증, contenteditable 주입 폴백, gemini Enter 전송·deepseek stop 보강.

## 9. 사용자 확정 결정 (2026-05-29)
- **M-3 1라운드 빈 context**: **"타 LLM 키만 빈 값 전송"** 확정(VER-002 14-1 리터럴 준수). → 216/215/220/213 현 기본 동작과 일치, 추가 수정 없음. opt-out 문구는 미래 변경 옵션으로 정보만 남김(현 기본 행동에 영향 0).

## 11. 세션 마무리 (표준 섹션 정렬)

- **세션 목적**: `210.설계산출물-구현적용` 최종 검토 + 발견 이슈를 설계문서에 직접 반영하여 구현 진입 가능 상태 확정.
- **진단 결과**: C-1(stale closure), C-2(첫전송 단락평가), M-1(재사용 라벨 오분류), M-2(selector 단일 출처 모순), M-3(14-1 리터럴 미준수), M-4(tokenizer interop) + critic 추가 H2/H3/M1/M2 — 11건.
- **처방**: 210 문서 12개 전수 개조(§3 참조). M-3 사용자 확정 결정 1건 반영(§9).
- **검증**: grep 스윕 옛 패턴 0건 / 신규 식별자 8문서 enumerate / jotai Provider 부재 검증 / uw-critic 1패스 통과(§10).
- **변경 파일**: 210/{211, 212, 213, 214, 215, 216, 217, 218, 219, 220, 221, README.md} 12개(§4).
- **다음 단계**: Phase 0 빌드 게이트 → Phase 1 정리 + Phase 2 PoC(6사이트 단발 왕복) 병렬 → Phase 3~8 순차. runtime 실증 영역(사이트 DOM 거동)은 PoC에서 1~2 라운드 튜닝으로 해소 예상.

## 10. 구현 전 최종 게이트 (2026-05-29) — PASS
- 전 문서 grep 스윕: 옛 패턴(`draft.status`/`setSession`/`yaml.stringify`/`bot.sendMessage({prompt,signal})`/`engine.start() &&`/gemini `generatingFlag(div.thinking)`/`useChat 내부 'Cancelled'`) **잔존 0건** 확인.
- 신규 식별자 enumeration: `maxRoundsAtom`/`tokenLimitAtom`/`LLM_TAB_NOT_FOUND`/`DOM_SELECTOR_FAILED`/`TOKEN_LIMIT_EXCEEDED`/`yarn add yaml`/`'downloads' permission`/`deepseek.png`/`types/round.ts` 모두 다중 문서에서 enumerate.
- jotai `<Provider>` 베이스코드 부재 확인(`grep`) → `useStore()` 기본 store 반환, C-1 fix 가정 성립.
- 마지막 정합 보정: 217 §3.2 [G2] draft json gemini entry 갱신(generatingFlag 제거·stopButton 갱신), 212:3 분류 정의 [M-1] 정합, 219 §2 E2E #6의 'Cancelled' 잔여 표현을 [M2 critic] 표현으로 교체.
- 게이트 판정: **Phase 0 진입 가능(GO)**.
