# 세션 정리 — 2026-05-28

## 참여
- 사용자
- **opencode** (모델: `deepseek-v4-flash-free`) — 설계 산출 세션 (= "DeepSeek 협업")
- **Claude Code** (이 세션) — 요구사항정의서 작성 · 산출물 검토 · 결함 수정 · 세션 복구

> 본 정리는 opencode 세션 2건의 복원 transcript + 이 Claude Code 세션을 종합한 것.
> 원문: `900.AICOWORK/20260528/transcript-기본핵심종합-파일생성요청.md`(292메시지), `transcript-다중LLM채팅방설계계획.md`(89메시지)

---

## 1. 오늘 한 일

### 1.1 opencode 설계 세션 (DeepSeek)
- `100.START/` 생성: `10-기본.md`, `20-핵심.md`, `30-종합.md` (5개 LLM 답변 검토 후 정리)
- `200.설계산출물/` **7종 설계서** 생성: `201-요구사항정의서`, `210-시스템-아키텍처`, `220-데이터-모델`, `230-라운드-상태천이`, `240-메시지-인터페이스`, `250-LLM-Adapter-명세`, `260-UI-와이어프레임`
- 설계 추가/변경에 **`[v2]` 변경이력 마킹 체계** 도입 (원본 유지 + 설계서에 이력)

### 1.2 Claude Code 세션 (이 세션)
- `100.START/요구사항정의서.md` 작성 (원문 13항목 → FR-01~FR-13 구조화, 원본 유지 원칙)
- `100.START` + `200.설계산출물` **교차 검토 → 16건** 도출 (불일치/결함/경미)
- **문법·구조·표준 결함 수정** (원본 `100.START`는 무수정):
  - `220` JSON 쉼표/중복펜스 복구 + 메타데이터 **표준 JSON화**(주석→캡션) + §3/§5
  - `260` UI 와이어프레임 **HTML 구조 복구** (고아 블록·잉여 `</div>` 제거, div 균형)
  - `240` ERR_003(타임아웃 모순)·ERR_005(토큰 하드코딩)·SEND_PROMPT payload 정합
  - `201` No 컬럼 매핑 교정·개수 보정·**§6 LLM 종수 연혁 모순 정리**
  - `230` SAVE_MD 저장 시점(중간/STOP 종료 포함)을 UI와 정합
- **opencode.db에서 설계 세션 2건 읽기전용 복구** → transcript md로 저장

---

## 2. 확정된 핵심 설계 결정

| 항목 | 결정 | 비고 |
|------|------|------|
| 대상 LLM | 정의 **11종**(선택 풀) → **우선 출시 6종** (ChatGPT·Claude·Gemini·DeepSeek·Perplexity·Grok) | 10종→manus 추가·le chat=mistral 별칭(11종)→DOM 부담으로 6종 축소 (순차 결정) |
| 동시 활성 LLM | 최대 **6개** (2×3 그리드) | 6→8 확대했다 **우선 6종에 맞춰 6 환원** [v3] |
| LLM 교체 | 세션 중 슬롯 **swap** 가능 (FR-14) | 각 셀 swap combo |
| STOP | **라운드만 중단, 재개(RESUME) 가능** | 자동 타임아웃 없음(수동) |
| 비밀채팅 | 아무 때나 시작, 1000토큰, **저장 안 함(휘발성)** | 슬롯 🔒 버튼 → 오버레이 |
| 토큰 제한 | UI에서 **조정 가능** (combo, 기본 1000) | maxRounds도 UI 조정 |
| 라운드 | 시작 시 **3~9** (기본 5) | |
| 전달 포맷 | **LLM 전달=YAML**(토큰 최소+시인성), 내부=JSON, context **최소화** | self-exclusion(자기 응답 제외) [v3] |
| 미로그인 LLM | 로그인된 것만 진행, **session 체크 제거** | prompt 실패 시 "로그인 필요" |
| MD 저장 | **세션 종료 시** (LLM별 + summary) | 자동저장 없음 |
| SW 생존 | 매 라운드 `chrome.storage.session` 저장 | MV3 SW 30초 종료 대비 |
| Manifest | **V3** | |

---

## 3. 산출물 / 파일

| 위치 | 파일 | 상태 |
|------|------|------|
| `100.START/` | 10-기본·20-핵심·30-종합·user-request·user_request·요구사항정의서 | 원본 (무수정) |
| `200.설계산출물/` | 201~260 (7종) | 검토·결함수정 완료 |
| `900.AICOWORK/20260528/` | transcript 2건 + 본 세션정리.md | 복구·신규 |

---

## 4. 미해결 / 후속 (설계 갭리뷰 기준)

| 리스크 | 대응 |
|--------|------|
| HttpOnly 쿠키로 session 체크 불가 | session 체크 제거 (적용) |
| MV3 Service Worker 30초 종료 | 매 라운드 storage.session 저장 (적용) |
| 다수 LLM DOM selector 유지보수 부담 | 우선 6종으로 축소 (적용) |
| **Manus는 채팅봇이 아닌 AI 에이전트 플랫폼** | ⚠️ 어댑터 적용 가능성 재검토 필요 |
| 다중 탭 동시 관리 + bot detection 위험 | 미해결 |

**다음 작업**: 기술 리스크 확인 → PoC/MVP 구현 진입.

---

## 5. 사고/교훈
- 오늘 opencode 설계 세션의 `세션정리.md`가 **실제로 저장된 적이 없어**(20260528 폴더 부재) 이력 분실로 오인. 세션은 archive조차 안 된 상태였고, 대화 원문은 `opencode.db`에 보존되어 있어 **읽기전용 추출로 전량 회수**함.
- opencode 저장소(`~/.local/share/opencode/opencode.db`)는 Claude Code(`~/.claude/projects`)와 **완전 별개** — 도구 간 이력은 교차 검색되지 않음.
