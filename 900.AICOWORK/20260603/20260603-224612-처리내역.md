# 20260603 처리내역 — ai-sarangbang P0·P1 구현 + 라이브 튜닝 + AI 자동대화(C3)

> VER-003 `ai-sarangbang`: 설계 완료 상태에서 **실제 구현 착수**(D3 확정) → P0(메커닉·드라이버·UI)·P1(로컬 실 AI) 완성 → Ollama 라이브 운용 중 문제 디버깅·튜닝 → AI 자동대화(C3) 추가. 구현 위치 `E:\workspace\ai-sarangbang`(독립 git repo). 정본=`220.설계산출물-ai-sarangbang/221~226`.

## 세션 목적
직전(~06-02) ai-sarangbang 설계(221~226) 완료, **D3 하드게이트만** 남김. 이번 세션: D3 확정 → P0 착수 → 동작하는 앱 → 실 AI → 사용자 요청 기능 반복 + 라이브 디버깅.

## 1. D3 확정 (P0 하드게이트 해소)
- 사용자 확정: **D3 = 사람 우선 abort**(제안값). 사람이 AI 발언 중 전송 시 현재 출력 abort(부분보존·stopped) 후 새 턴. [222] §5 single-flight 그대로 → S3 즉시 착수. 정본 반영(221 §4/§7, 226 §0).

## 2. P0 구현 (Mock·솔로·메커닉) — 226 S1~S7
- 스캐폴드: Vite+React+TS, vitest(jsdom), 버전핀(226 §2).
- **M1(헤드리스 메커닉)**: S1 types/id(nanoid6)/AgentDriver · S2 context(self포함·done만·whisperContext 정규화)/invariants(completedNaturally·휘발단언) · S2.5 store(useSyncExternalStore+자작 emitter, publish/onState/onWhisper/**onRoom**) · S3 coordinator(single-flight runLoop+pending+busy). **uw-critic 1차** C1(willSpeak throw→방 wedge)·H1(abort 무시 드라이버 stopped)·M2(whisper 대상검증)·L1(중복 emit) 수정+회귀. 31테스트.
- **M2**: S4 MockDriver(스크립트·char토큰·abort 즉시·순환)+withHardTimeout(idle 안전망)+timing · S5 md-export(결정적·공개만·중단 placeholder·slug). +18.
- **M3(동작 90s 사랑방)**: S6 UI(Room/MessageLine/Roster/Composer/WhisperPanel/SaveBar/colors/theme.css 하이텔네이비) + i18n(ko/en) + S7 main 와이어링. Typewriter=appendToken emit 렌더(자체 애니 없음, 222 §4.1). RTL 게이트 + Room 엔드투엔드.
- 검증: 65 green · tsc strict 0 · prod build · `npm run dev` 서빙 · **core 순수성(React/DOM 0)·UI 한글리터럴 0** 게이트. 정본 222 v3.1(onRoom·hooks 객체화), 226 진척.

## 3. 독립 git repo
- `E:\workspace\ai-sarangbang`가 상위 `E:\workspace`(빈 .git) 안 하위였음 → 사용자 선택 **(a) 독립 git init**. 커밋 08b7be8(M1·M2)→9bb7aa9(M3)→4f166b4(gitignore: tsbuildinfo/emitted vite.config).

## 4. 멀티-휴먼 "정적 자원만" 질의 → A(솔로) 확정
- 답=원칙적 불가(실시간 공유상태·단일 floor권위는 쓰기+push 백엔드 필요; 정적 호스팅은 읽기전용). 스펙트럼(솔로 / 무료 BaaS / Durable Objects·PartyKit / Node+WS) 제시. **사용자: A(솔로) 우선** → follow-up으로 둠.

## 5. P1 — OllamaDriver (로컬 무료 실 AI) — 224 §3
- **드라이버 교체만**(main 한 줄)로 실 AI. createOllamaDriver(/api/chat stream NDJSON→토큰 yield, signal→fetch abort, persona/roster→messages). **coordinator.ts·src/ui diff 0** 입증(AgentDriver 추상화 검증, git diff로 확인).
- SpeakContext에 **roster** 추가(드라이버 id→이름 라벨링·seam 자기완결·멀티유저 대비).
- CORS=Vite proxy `/ollama`→localhost:11434(설정만, 키 없음). config.driver 'mock'|'ollama'(기본 mock). 76테스트.
- **per-AI 모델**: model resolver(string|(p)=>string)+think옵션(비-thinking 모델 안전). 라이브 검증(exaone/gemma2/qwen3 think:false 한국어 생성·사고누수0). 78테스트. 커밋 cf190f6→7a2aee8.

## 6. 봇 이름=모델명 + 속도 경쟁 floor(①) + 이름 메아리
- persona 제거(모델 기본 voice) / 봇 이름=모델명(EXAONE/Gemma2/Qwen3).
- **핵심 재설계 — 속도 경쟁 floor**: 사용자 의도="먼저 응답한 LLM이 선점"([221] R2 원문)인데 P0가 좌석순 FIFO로 단순화([M-1]·[C5] 미뤘던 동적)돼 있었음. → coordinator floor를 **race로 재작성(①: 첫토큰 선점+재생성)**: 전원 동시생성→첫 토큰 선점 승자 발언, 나머지 abort→남은 후보 재경쟁/재생성. raceRound/pullFirstToken/streamWinner 신설, enqueue/queue/drainFloor 제거. 바지인(master abort)·single-flight·whisper 유지. coordinator.test 전면 재작성(속도순·재생성호출·floor직렬·바지인·무응답드랍). + stripLeadingSelfLabel 래퍼(이름 메아리 제거·스트림 안전). 커밋 fbbefcc. 87테스트.

## 7. 라이브 운용 디버깅·튜닝 (Ollama 실측)
- **긴 context 무응답/느림**: 조선물류 문서(~1.5k토큰) 붙여넣자 무응답. 원인=긴 prompt-eval × 3모델 동시 race × 89% RAM + 실패모델 **조용한 드랍**으로 무응답이 안 보임. LLM stateless 설명(매 요청 messages만; 마지막만 보내면 맥락상실).
  - **맥락 윈도우 절삭(M4)**: buildSpeakContext에 ContextLimit(maxMessages·maxChars)→최근분만 전달, 누적 폭증 차단. config.contextWindow. 커밋 eca670b→5d59559(임시 _t.json 정리·gitignore). 90테스트.
  - **실패 표식**: 드랍 모델을 status=error 빈메시지로 publish→UI `[모델] (응답 없음)`. MessageLine error+빈텍스트=응답없음/부분텍스트=응답오류. 커밋 9735554. 91테스트.
- **귓속말 무응답**: 공개 race가 Ollama 점유→같은 모델 귓속말 직렬대기→30s 타임아웃([C2] 경합). idle일 때 정상 확인→귓속말 로직 정상.
- **Gemma2(9B) 잦은 드랍**(최중량=경쟁 발목; roster 무표시=idle=드랍 판독) → 사용자 요청 **Gemma2→Phi4-mini(~3.8B)** 교체. 커밋 b95ddd1. 윈도우 1500/8 조임(50c123d). RAM 진단=3모델 ~18GB/32GB(89%).

## 8. AI 자동 대화 모드 (C3) — "나 없이 진행"
- 사용자 요청+검토 → **권고안 채택**. [222] C3([221] D1 대안). "자동 진행/멈춤" 토글. 한 AI씩 **라운드로빈**(직전 대화 반응)·턴간 3.5s·최대 8턴 후 자동정지. **사람 입력 시 자동정지(우선)**. coordinator startAutoMode/stopAutoMode/runAutoTurn/streamMessage/interruptibleDelay+runLoop 통합(기존 race/human **무변경·추가형**)+onAuto hook. config.auto. store autoActive. Room 토글버튼. 커밋 95fd41d. **95테스트**.

## 9. 학습 가능성 질의(개념)
- "대화 자주→로컬 학습?": **아니요**(추론 stateless·가중치 불변). 로컬 fine-tune은 NVIDIA GPU 필요(Intel iGPU 비현실)+잡담 학습 비권장. "기억"=학습 아니라 **영속화+RAG**(follow-up).

## 검증 총괄
- 최종 **95 Vitest green(16파일)** · tsc strict 0 · vite build green · `npm run dev` 동작 · core순수성/UI한글0 게이트. 라이브: exaone/gemma2/qwen3/phi4-mini 프록시 경유 실 생성 확인.

## 커밋 (ai-sarangbang repo, main)
08b7be8 M1·M2 → 9bb7aa9 M3 → 4f166b4 gitignore → cf190f6 P1 → 7a2aee8 per-AI → fbbefcc race floor(①) → eca670b 맥락윈도우 → 5d59559 정리 → 9735554 실패표식 → 50c123d 윈도우조임 → b95ddd1 Phi4-mini → 95fd41d 자동대화.

## 메모리 정합 (중요)
- `feedback_multiaichatting_design_only`(2026-05-29)는 **v1 multichat Chrome확장(`E:\workspace\multichat`)** 한정 "구현은 사용자가 직접". **VER-003 ai-sarangbang(신제품·로컬 웹앱·`E:\workspace\ai-sarangbang`)은 이번 세션 사용자가 명시·반복 구현 지시 → 구현 활성**. 피드백 메모리 범위 갱신함(공통 memory + 인덱스).

## 다음 단계 (resume)
- **자동 대화 모드 라이브 테스트**(사용자 본인 터미널 `npm run dev`, 세션 무관 지속; cmd 드라이브전환 `cd /d`).
- (선택) 자동 모드 **uw-critic 점검**(single-flight·바지인·interruptibleDelay 엣지).
- P1 튜닝: 모델 선택·프롬프트(이모지·길이)·num_ctx(긴 입력).
- follow-up: 대화 **영속화+RAG**("기억" 체감, GPU 불요) · 멀티유저(BaaS/Durable Objects — 정적-only 불가) · P2 ApiDriver(유료·최후).
