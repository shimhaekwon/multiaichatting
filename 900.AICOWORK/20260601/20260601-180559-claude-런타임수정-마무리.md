# multichat 런타임 디버깅 수정 — 마무리 (최종)

> **적용일**: 2026-06-01 18:05
> **대상**: `E:\workspace\multichat` (ChatHub v1.45.7 개조, MV3)
> **수정 주체**: Claude (Opus 4.8)
> **검증**: `yarn build` ✓ · `tsc --noEmit` 0 · 런타임 1종/2종 라운드 E2E 동작 확인
> **상태**: 진단 로그(`DEBUG`) off. 코드 수정 완료.

---

## 1. 적용된 수정 (전체)

| ID | 문제 | 파일 | 수정 |
|----|------|------|------|
| **ERR_002** | 새 탭 `promptArea` 렌더 전 "로그인 필요" 오발 | `content-script/dom-adapter.ts` | `promptArea` 폴링 대기(최대 3초) |
| **RT-1** | done 자동 감지 실패 → STOP 수동 필요 | `dom-adapter.ts` | done = stop 소멸 **OR** 텍스트 안정화 |
| **stream** | abort 시 `Cannot close an errored readable stream` | `app/bots/abstract-bot.ts` | `settled` 가드(close/error 1회 보장) |
| **BG-TAB** | 비활성 탭 `setInterval` throttle → 탭 열어야 응답 넘어옴 | `dom-adapter.ts` | `observeResponse`를 **MutationObserver** 기반으로 전환(+100ms throttle, 보조 타이머) |
| **U-1** | 🔒 비밀채팅 Hooks 규칙 위반 크래시 | `SecretChatOverlay.tsx` | `useCallback`→일반 함수 |
| **U-5** | 🔒 제목 이중표기 | `SecretChatOverlay.tsx` | 코드 측 🔒 제거 |
| **U-3** | swap 미결선(드롭다운 미렌더) | `MultiBotChatPanel.tsx` | `onSwitchBot={(b)=>engine.swap(s.slot,b)}` |
| **U-2** | i18n 기존 ChatHub 키 소실 → 원시 키 노출 | `i18n/locales/{ko,en}.json` | `Stop·Send·Share conversation·Clear conversation·View history·GitHub·Feedback·Display·Settings` 추가 |
| **FEAT** | 요청/완료 시각 표시 | `types/round.ts`·`use-round-engine.ts`·`CellStatusBadge.tsx`·`ConversationPanel.tsx`·`MultiBotChatPanel.tsx` | `RoundRecord.requestedAt` + 봇별 완료시각(`completedAt`) + 셀에 `⏱ 요청 … · 완료 … (N.Ns)` |
| 진단 | `[MC]` 디버그 로그 | `dom-adapter.ts` | `DEBUG` 플래그(최종 **false**) |

> Plan v3(C1 STOP DONE·bg cleanup·M1 재시도·M2 proxy-fetch 삭제)는 OpenCode 적용분으로, 본 세션에서 반영·검증 완료.

## 2. 검증
- `yarn build` ✓ (built ~8.6s) / `tsc --noEmit` EXIT 0
- 런타임: **ChatGPT 1종**, **ChatGPT+Claude 2종** 라운드 — 응답 수신 · **자동 종료(ROUND_COMPLETE)** · 콘솔 무에러 확인
- 옛 빌드의 내부 API CORS · stream TypeError · ERR_002 모두 해소 확인

## 3. 디버깅 핵심 교훈
- 초기 에러 다수의 진짜 원인은 **옛 빌드(내부 API 봇) 미교체 + `app.html` 탭 미재오픈**이었음. → **삭제 후 `dist` 재로드 + app.html 탭 재오픈 + LLM 탭 F5**가 선행돼야 새 코드가 실린다.
- 비활성 탭 `setInterval` throttle은 **MutationObserver**로 회피.
- 번들 해시(`app-xxxx.js`)로 새 빌드 반영 여부를 즉시 판별 가능.

## 4. 잔여 / 미결정 (사용자 보류 또는 저우선)
- **탭 정리**(그룹 접기 / 별도 창 / 종료 시 닫기) — 보류. *완전 비표시는 DOM 방식상 불가.*
- **내부 API 방식(탭 없음, 4.1.1 수준) vs DOM 유지** — DOM 유지 권고(안정성). 원본 ChatHub이 탭을 안 여는 건 내부 API 방식이기 때문이며, 1.45.7 내부 API는 현재 사이트와 불일치(222 결정).
- **participants 필터** — `context-builder`가 prevRound 순회 시 현재 참여 필터 없음(swap 시 과거 LLM 혼입 가능). 견고성 패치 미적용.
- **U-4 stoppedBanner 미렌더 · U-6 세션종료 버튼/진행바/전체선택** — 저우선.
- "Gemini/DeepSeek 응답 등장" = **LLM 환각**(멀티챗 context는 정상). 멀티챗 버그 아님.
- 1R 빈 context UX(opt-out 권장) — 미적용.

## 5. 후속 권장
- 멀티챗 소스 **git 버전관리 시작**(현재 커밋 이력 없음) — 수정 추적·롤백 안전망
- 2~6종 + **R2 교차전달(YAML)** · STOP · 비밀채팅 · swap · DONE 후 MD 저장 E2E 검증
- `800.테스트`의 IT/TC 시나리오로 회귀 검증

---

## 부록. 협업/수정 문서 (20260601)
| 단계 | 문서 |
|------|------|
| ① 1차 구현검토 | `121916-claude-구현전-검토결과` |
| ② Plan v2 | `122721-deepseek-구현전-검토결과` |
| ③ Plan 검토 | `123624-claude-수정Plan검토결과` |
| ④ Plan v3 | `124035-deepseek-수정Plan검토결과-reply` |
| ⑤ 구현 후 최종검토 | `125845-claude-구현후-최종검토결과` |
| ⑥ 수정 적용 | `165054-claude-수정적용내역` |
| **⑦ 런타임 수정 마무리(본 문서)** | **`180559-claude-런타임수정-마무리`** |

*Claude(Opus 4.8)가 코드 수정·빌드·타입검증 수행. 런타임 테스트는 사용자 진행(1·2종 동작 확인).*
