# 처리내역 — 210 설계산출물 최종검토(3단계) + 구현 blind 가능화

- 일시: 2026-06-01 08:35
- 작업: `210.설계산출물-구현적용` 최종 검토 → 발견 이슈 설계문서 직접 반영. 최종 목표 = **다른 LLM이 210만 보고 구현 시 설계 질문 0**.
- 모드: 1·2차 일반 검토 → 마지막 ultrawork(업무처리원칙 v4 + 코드생성원칙 §4 3라운드 inspection)
- 베이스 대조: 요구 `100.START/VER-001·002`, 코드 `E:\workspace\multichat`(ChatHub)

## 1. 진행 단계 (3패스)

### 1차 — 정확성 검토 + H-1
- 5/30 변경 3문서(212/217/219) ↔ 나머지 9문서 정합성 대조 + 베이스코드 계약 직접 실증.
- **H-1 (HIGH, 다중 라운드 무동작) 발견**: `nextRound`(=`sendPrompt`)가 `ROUND_COMPLETE`에서 가드(`status!=='ROUND_ACTIVE' → return`)에 막혀 2라운드 진입 불가. "다음 라운드" 버튼·Enter 양 경로 무동작. 빌드/타입/1라운드 단위테스트는 통과 → Phase 5 E2E에서야 발현.
- **반영(옵션 A)**: 215 가드 완화(`ROUND_ACTIVE||ROUND_COMPLETE`), 215 §1 상태표·104 nextRound·218 §2 handleSubmit·§5 액션표·213 §5 흐름 정합화. (7곳)

### 2차 — 서브시스템 실증(3 에이전트) + 추가 발견
- manifest/background/storage.session/제거대상/의존성/컴포넌트 재사용 **전수 실증 → 212 파일매트릭스 매우 정확** 확인. `storage.session setAccessLevel` 등 주장 사실.
- 신규 MODERATE 3 + MINOR: **N-1**(진입점 모호: 그리드는 app.html `MultiBotChatPanel`인데 213/목업/Phase0은 "사이드패널" → 상충), **N-2**(비밀채팅 웹대화 격리 불가: all-DOM 단일탭 공유), **N-3**(swap이 `session.slots` 미갱신 → MD/snapshot stale), N-4(background 호출처 정리), N-5(tabs 권한 불요), N-6(sidepanel.tsx 프리미엄 게이트).
- **사용자 결정**: N-1 = **app.html 탭으로 통일**(사이드패널 폐기), N-2 = **한계 수용+명시**.
- **반영**: "사이드패널" 문구 전수→"app.html 탭" 정정(213×4·218×2·219×2·220×2·211·212·214), 217 §6 N-2 한계 명시, 215 swap N-3 동기, M-1(217 §3.2 초안 selector 삭제→221 §7 정본), M-2(onUserSendMessage optional), N-4/5/6. (~20곳)

### 3차 — ultrawork: blind 구현성 (질문 0 목표)
- **4 uw-explorer 병렬**(구현자 관점 "멈춰 물어볼 지점") → **실질 갭 ~27건** + 검증-안전 4건 도출(DERIVABLE/USER-DECISION/MISSING 분류).
- **사용자 결정**: 산출물 구조 = **신규 222 + 깨진코드 in-place + 포인터**.
- **`222-구현확정사항.md` 신규 작성**(20섹션): genId·RoundActionButton 계약+입력결선·gridCols 리터럴·isLoginPage·loadRoundSnapshot 결선·content-script 본문(fillPrompt/observe/findStop)·perplexity querySelector 교정·ensureTab·CHATBOTS 6종·ErrorCode·permissions verbatim·MD 결정성·1라운드 빈 YAML 정확형·비밀채팅(mount/토큰/스트림)·사이드바 가드+toast·i18n 키표(~30)·런타임 천장(§18)·follow-ups·검증안전. **211~221과 충돌 시 222 우선**.
- **in-place 교정**: 218 gridCols(동적→리터럴), 221 perplexity stop(:has querySelector throw→findStop 경로)+통합 JSON 정본 명시, 212 rules 전부제거·sidepanel 제거 확정·RoundActionButton 매트릭스 추가, README/211 "222 필독" 포인터, 217 §3.3·215 §4 포인터.
- **uw-critic 1패스 → 6건(CRITICAL 2 + HIGH 3 + MED 2)**: C-1(217 §3.3 STOP가 naive querySelector→perplexity throw), C-2(RoundActionButton onOpenSave arity 불일치→컴파일 깨짐), H-1(loginIndicator 인터페이스 누락), H-2(CS connect 경합), H-3(in-flight 재수화 미정의), MED(history-union 잔여 모순, sidepanel.html 빌드 문구). **전부 수정** + SaveSessionDialog/SecretChatOverlay 계약 보강.
- **self-검증으로 §7 [G5] 과단정 1건 추가 발견·수정**(스냅샷 형태만 대체, chatFamily 재구성은 유지).
- **uw-critic 최종 재검증 → YES 판정**: 6건 RESOLVED, 새 컴파일/모순 0. 잔여 = §18 런타임뿐. + LOW(217 릴레이 재시도 포인터) 닫음, 날짜 stamp 2건 06-01 정정.

## 2. 변경 파일 (210 — 누적)
211, 212, 213, 214, 215, 216, 217, 218, 219, 220, 221, README + **222(신규)**. (216은 1·2차에서 기반 확인, 직접 편집 최소)

## 3. 핵심 검증 결과 (베이스코드 실증 통과)
- useChat.sendMessage(string)·chatFamily.messages·AbstractBot{onEvent,Event}·jotai Provider 부재·ConversationPanel props·SixBotChatPanel·errors.ts·deps(yaml 부재→추가, gpt3-tokenizer/jotai-immer/react-hot-toast/browser-fs-access/nanoid 존재)·esModuleInterop:false·resolveJsonModule:true — 전부 설계와 일치.
- 222 고위험 주장 critic 검증: genId/nanoid customAlphabet ✓, ChatMessageInput=`<form>`이라 type=submit actionButton이 onSubmit 발화 ✓, yaml v2 `{a:''}`→`a: ""` ✓, 로고 에셋 존재 ✓.

## 4. 완료 판정
- ✅ **구현 blind 가능**: 다른 LLM이 210(특히 222)으로 설계 질문 없이 구현 가능 — uw-critic YES(§18 런타임 제외).
- ✅ 회귀 0: 수정 델타 독립 critic 재검증 통과.
- ⚠️ **천장(불변)**: 6사이트 실제 DOM 거동·contenteditable 주입·Gemini Enter·DeepSeek stop은 문서로 0 불가 → Phase 2 PoC(221 §1 절차)로 구현자 자가확정. 비밀채팅 웹격리는 한계 수용(사용자 확정).
- 코드 미작성이므로 6.3 post-push 실증은 N/A(설계 단계). 다음 = Phase 0 빌드→1 정리→2 PoC.

## 5. 6-6 원칙 회고
- ① 빠짐없이: 12문서 전수 + 4 explorer로 구현자 질문지점 enumerate, 27갭 + critic 6 + self 1.
- ② 틀림없이: 모든 계약·라인·selector를 베이스코드/요구 실측 대조. 날짜 stamp 오차도 정정.
- ③ 더함없이: 설계 명료화만. 신규 기능 0(222는 검토 산출물). follow-up은 §19에만.
- ④ 다름없이: AbstractBot/Jotai/ChatHub 패턴·기존 numbered-doc 구조(222) 미러링.
- ⑤ step by step: 1차→2차→ultrawork(plan→explore→fix→critic→재critic).
- ⑥ no guess: N-1/N-2 등 제품 갈림은 사용자 결정 받음. 기술 기본값은 요구/베이스/기존결정에서 derivable한 것만 확정.
- ⑦ challenge: "질문 0의 천장(런타임)" 선제 명시, 수정이 새 흠 유발 가능성을 self+critic 2중 검증으로 입증·해소.

## 6. 다음 단계
- Phase 0: `corepack enable && yarn install && yarn add yaml` → build → app.html 탭 구동.
- Phase 1: 222 §2 제거 확정대로 정리(sentry+main.tsx, server-api, rules, sidepanel) → tsc 0.
- Phase 2: 221 §1 절차로 6사이트 단발 왕복 PoC → adapter-config.json version=1 확정.
