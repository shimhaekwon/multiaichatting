# 처리내역 — OpenCode DeepSeek 설계검토 의견 검증 + 입력패키지(223) 신규

- 일시: 2026-06-01 10:08
- 작업: OpenCode DeepSeek가 전달한 "210 설계산출물 구현 이슈"(Critical 5 + Moderate 4 + Minor 2)를 정본·베이스코드 실측 대조 검증 → 진짜 갭만 222 반영 + 구현 LLM 입력 누락 재발 차단용 223 신규.
- 베이스 대조: 설계 `210.설계산출물-구현적용/211~222`, 코드 `E:\workspace\multichat`(ChatHub v1.45.7)

## 1. 세션 목적
- OpenCode의 DeepSeek가 보고한 설계 이슈(C1~C5/M1~M4/m1~m2)가 **실제 설계 결함인지** 판정.
- 사용자 후속 요청: "구현 시 DeepSeek 이슈가 없어질지" 검토 → 없애는 조치 실행.

## 2. 진단 결과 (12문서 + 베이스코드 실측)
- **C1~C5(Critical) 전부 "이미 해결"**: 근본원인 = DeepSeek가 **221(셀렉터 실측)·222(확정사항)를 안 읽음**(입력 누락). 원문에 "설계문서(221)에 있었다면" 가정법 = 미열람 자백.
  - C1 딥시크봇: 신규 대상(212 §9·217 §3.2·222 §4·221 §7 셀렉터 실측 완비). "5봇 축소"는 6봇 확정 설계 위반 → 거부.
  - C2 keep/delete: 212 §2가 재작성/신규/제거로 정확 분류. DeepSeek 자기 9단계의 "유지" 오분류가 혼란원.
  - C3 셀렉터 없음: **사실오류** — 221 §7에 6종 실측 통합 JSON(2026-05-29) 존재.
  - C4 메시징: 추천(port-based)이 옳고 **217 §3·222 §9에 이미 확정**(경합 3회 재시도까지).
  - C5 저장소: **이미 설계** — session-store.ts 스키마 214 §5+222 §7. chat-history.ts(실재)는 셀 기록용 별개.
- **M2·m2 = 진짜 문서 갭(코드 실측 확인)**:
  - M2: `Sidebar/index.tsx` getPremiumActivation(L17·49)·api.checkDiscount(L50)·PremiumEntry(L25·98)·showDiscountModalAtom·open-times **실재**. premium 결합 src 전체 33곳. 211 §4 "Premium/*"·212 §8에 Sidebar 하위 PremiumEntry.tsx 미열거.
  - m2: `ErrorAction.tsx:18` `location.href.includes('sidepanel.html')` 실재 → sidepanel 폐기 후 dead code.
- **M3 부분 타당(cosmetic)**: premium i18n orphan 키 정리는 real이나 빌드 무영향. "삭제봇 6키"는 **무효**(봇명은 i18n 아니라 consts.ts CHATBOTS).
- **M1·M4·m1 정확하나 이미 반영/minor**: M1 host_perm=222 §3, M4 gpt3-tokenizer=216 기존, m1 process=소스 import 0(devDep만).

## 3. 처방 (변경 실행)
- **222 편집 3곳**: §1[m2] ErrorAction dead code 제거 / §2[M2] Sidebar premium 결선 제거+PremiumEntry.tsx 삭제 / §17[M3] orphan 키 cosmetic + 봇명 i18n 오해 반박.
- **223 신규**: 구현 LLM blind 구현용 입력 패키지 — §1 프롬프트 전문(절대규칙 8: 222>221 우선·셀렉터추측금지·port고정·저장소재설계금지·6봇고정·파생물불신), §2 입력 매니페스트(줄것=210+베이스 / 주지말것=9단계요약·200초안 반례명시), §3 Phase별 정본포인터+흔한오진, §4 선답FAQ(C1~C5 정본위치 선답).
- **README**: 13번 항목(223) 등록.

## 4. 검증
- 베이스코드 실측: services 폴더 구성·Sidebar/index.tsx 라인·ErrorAction.tsx:18·components/Dialog.tsx·use-chat.ts·abstract-bot SendMessageParams/onEvent 계약 전부 일치 확인.
- 편집 자가검증: sidebarCollapsedAtom 미변경(유지), use-premium.ts 삭제는 212 §4 기존 정합, 새 모순·중복지정 0.
- **최종 답**: "이슈 없어지는가 = 조건부 YES". 원인 둘(문서갭 M2/m2/M3 + 입력누락 C1·C3·C4·C5) 모두 닫힘. 입력패키지(223)로 시작 시 C1~C5는 §4 FAQ로 선답돼 질문 자체가 안 나옴. 잔여 = 222 §18 런타임 천장(221 §1 DevTools 자가확정, uw-critic 동일 결론).

## 5. 변경 파일
- `210.설계산출물-구현적용/222-구현확정사항.md` (편집 3곳: §1·§2·§17)
- `210.설계산출물-구현적용/223-구현LLM-입력패키지.md` (신규)
- `210.설계산출물-구현적용/README.md` (13번 등록)

## 6. 다음 단계
- Phase 0 진입(corepack enable && yarn install && yarn add yaml → build → app.html 탭 구동).
- 다른 LLM/세션에 구현 위임 시: **223 §1 프롬프트 전문을 첫 입력으로** 전달 + 210 폴더 접근 보장 + 9단계요약/200초안 제외.
- 잔여: 222 §18 런타임 천장(6사이트 DOM·contenteditable·Gemini Enter·DeepSeek stop)은 Phase 2 PoC(221 §1)로 구현자 자가확정.
