# multichat 구현 후 최종 검토 결과

> **검토일**: 2026-06-01 12:58
> **대상**: `E:\workspace\multichat` (ChatHub v1.45.7 개조, Chrome MV3) — **Plan v3 적용 완료본**
> **구현/수정 주체**: OpenCode (DeepSeek)
> **검토 주체**: Claude (Opus 4.8)
> **검토 기준**: `222-구현확정사항`(최우선) · 219 · 220 · 217 · 218
> **선행 문서**: 121916(1차검토) · 122721(Plan v2) · 123624(Plan검토) · 124035(Plan v3)
> **종합 판정**: **Plan v3 백엔드 수정 = 합격(5건 정확·회귀 0).** 단 전체 검토로 **UI 레이어 신규 CRITICAL 1 + MEDIUM 3** 발견(Plan v3와 무관한 기존 Phase 5 결함).

---

## 1. Plan v3 수정 검증 — 5건 모두 정확 반영 ✅

| 항목 | 수정 내용 | 위치(확인) | 판정 |
|------|-----------|-----------|------|
| C1-a | `stopObserve` hoist + STOP에서 timer 중단 + `DONE` 통보 | `content-script/dom-adapter.ts:93,110,117-122` | ✅ |
| C1-b | abort catch에서 port-dead 시 `onEvent(DONE)`+`cleanup()` | `app/bots/dom-adapter-bot.ts:25-32` | ✅ |
| bg-cleanup | `cleanup`에서 `appPort.postMessage(ERROR/ERR_002)` 종료 통보(CS 탭 종료 hang) | `background/index.ts:112-116` | ✅ |
| M1 | `connectToCSWithRetry` `await result` + `!==null` 직렬 재시도 | `background/index.ts:43-56` | ✅ |
| M2 | `proxy-fetch.ts` 삭제 | `services/proxy-fetch.ts` (EXISTS:False) | ✅ |

> double-DONE 가드는 Plan v3에서 "port.disconnect 동기 차단으로 불필요"로 판정·미반영 — 타당(동의).

## 2. 로직 유틸 직접 검토 ✅

| 파일 | 확인 | 판정 |
|------|------|------|
| `context-builder.ts` | self-exclusion · 1R 빈 context(`others[llm]=''`) · YAML · soft 지시문 · GPT3Tokenizer ESM/CJS interop | ✅ |
| `md-export.ts` | LLM별 흐름(2R+ 타LLM 블록) / summary 라운드순 전원 원문 / 미응답 `_(응답 없음)_` | ✅ (경미: `download` 후 즉시 `revokeObjectURL` — md 소형이라 무해) |
| `session-store.ts` | persist(session+local) · `loadRoundSnapshot` **WAITING→STOPPED 정규화**(HIGH-3) · clear | ✅ |

## 3. 회귀 — 없음
- dom-adapter STOP 보강은 정상 done 경로 불변(상위집합). bg cleanup ERROR는 `csPort.onDisconnect`(CS 탭 종료)에서만 발동 → 정상 완료/STOP 흐름엔 미발동(첫 DONE에서 app이 port.disconnect, csPort는 유지되어 cleanup 미트리거). M1은 성공 시 동일.

---

## 4. UI 레이어 신규 발견 (전체 검토 성과)

> ⚠️ 아래는 모두 **11시대 수정분(Phase 5 UI)** = 이번 Plan v3(백엔드)와 **무관한 기존 결함**. 1차 검토(백엔드 위주)에서 미탐지된 것을 전체 검토로 확정. **3건 파일로 직접 검증.**

| ID | 심각도 | 결함 | 위치 | 근거/명세 |
|----|--------|------|------|-----------|
| **U-1** | 🔴 CRITICAL | **Hooks 규칙 위반** — `if(!secretData) return null`(L19) **뒤**에 `useCallback`(L29). 오버레이 항상 마운트(`MultiBotChatPanel:115`)+atom 초기 null → 🔒 클릭 시 훅 수 증가 → **React 크래시** + `react-hooks/rules-of-hooks` 빌드 에러 | `SecretChatOverlay.tsx:19,29` | React 훅 규칙 |
| **U-2** | 🟡 MEDIUM | **i18n 기존 키 소실** — ko/en이 신규 32키만 남고 ChatHub 원본 키 삭제. `ConversationPanel`의 `t('Stop')`/`t('Share conversation')`·`Sidebar`의 `t('Settings')` 등 폴백 없어 **원시 키 노출** | `i18n/locales/ko.json`(32키), japanese.json=101키 | 222 §17 "기존 키 재사용" 위반 |
| **U-3** | 🟡 MEDIUM | **swap 미결선** — `MultiBotChatPanel`이 `ConversationPanel`에 `onSwitchBot` 미전달 → swap ▼ 미렌더 → `engine.swap()` 도달 불가(FR-14★ 미작동) | `MultiBotChatPanel.tsx:90-101` | 222 §2 · 218 §1 |
| **U-4** | 🟡 MEDIUM | **stoppedBanner 미렌더** — STOPPED 경고배너 미표시(키만 존재) | `MultiBotChatPanel` | 218 §5 |
| **U-5** | 🟢 LOW | SecretChatOverlay: 제목 **🔒 이중표기**(코드+키값) · `botRef` 렌더 중 생성(impure)·targetLlm 변경 시 stale · `AbortController` 지역변수→닫기 시 in-flight 미취소 | `SecretChatOverlay.tsx:25,34,65` | 견고성 |
| **U-6** | 🟢 LOW | 세션종료 버튼 · 진행바(`RoundProgress`) · 전체선택 체크박스 미구현 | SettingsBar/Sidebar | 218 목업 |

> 심각도 조정: 협력 에이전트는 🔒 이중표기(U-5)·i18n(U-2)을 CRITICAL로 분류했으나, 🔒 이중은 표시 무해(LOW), i18n은 기능 동작·표시 결함(MEDIUM)으로 하향. **U-1만 진성 CRITICAL**(런타임 크래시).

## 5. UI 통과 항목 ✅
RoundActionButton 상태분기·`type`(submit/button) 구분 · gridCols 리터럴(222 §11) · SettingsBar 옵션 {3,5,7,9}/{100…10000}·세션 중 잠금 · Sidebar 6종+최소1/최대6 toast·중복차단 · SaveSessionDialog 계약(session.slots/summary) · CellStatusBadge 매핑 · 비밀채팅 휘발성(저장 누수 없음) · Toaster 단일 · 세션복구(loadRoundSnapshot) · getCellStatus 공식.

---

## 6. 결론 & 수정 우선순위

- **Plan v3(C1/bg-cleanup/M1/M2) = 합격.** 정확히 반영, 회귀 없음.
- **전체 검토로 UI 기존 결함 발견** → 출시 전 별도 수정 필요.

| 순위 | 항목 | 조치 |
|------|------|------|
| **P0** | U-1 Hooks 위반 | `useCallback` 제거 또는 early-return을 훅 뒤로 이동 / 부모서 `{secret && <Overlay/>}` 조건 마운트 |
| **P1** | U-2 i18n | ko/en에 ChatHub 원본 키 **머지**(삭제 복원), 신규 32키 유지 |
| **P1** | U-3 swap | `MultiBotChatPanel`에서 `onSwitchBot={(b)=>engine.swap(i,b)}` 전달 |
| **P2** | U-4 stoppedBanner | STOPPED 시 상단 경고배너 렌더 |
| **P3** | U-5/U-6 | 견고성·목업 보완(선택) |

### 검증(수정 후)
1. `yarn build` / `tsc --noEmit` 0 + **`react-hooks/rules-of-hooks` 에러 0(U-1)**
2. 🔒 비밀채팅 열기 → 크래시 없이 동작(U-1)
3. ko 로케일에서 셀 헤더 툴팁·Sidebar 항목 한글 표시(U-2)
4. 슬롯 헤더 swap ▼ 노출·교체 동작(U-3)
5. `800.테스트`의 IT-02(STOP)/IT-03(CS 탭 종료)/TC-ERR-03(M1)

---

## 부록 A. 검토 커버리지

- **직접 검토(Opus)**: dom-adapter · dom-adapter-bot · background · use-round-engine · use-chat · abstract-bot · state/round · context-builder · md-export · session-store · manifest · SecretChatOverlay · MultiBotChatPanel · ko.json
- **에이전트 검토(검증됨)**: RoundActionButton · SettingsBar · CellStatusBadge · SaveSessionDialog · Sidebar · ConversationPanel
- **미직접검토(저위험)**: Button · Dialog · Layout · ChatMessageInput · errors.ts · types/index.ts · en.json(에이전트 인용) · adapter-config.json(1차 확인) · bots/index(1차 매핑)

## 부록 B. 협업 사이클 문서

| 단계 | 문서 | 작성 |
|------|------|------|
| ① 1차 구현검토 | `121916-claude-구현전-검토결과` | Opus |
| ② Plan v2 | `122721-deepseek-구현전-검토결과` | DeepSeek |
| ③ Plan 검토 | `123624-claude-수정Plan검토결과` | Opus |
| ④ Plan v3 | `124035-deepseek-수정Plan검토결과-reply` | DeepSeek |
| **⑤ 구현 후 최종검토(본 문서)** | **`125845-claude-구현후-최종검토결과`** | **Opus** |

---

*검토 산출물. Plan v3 백엔드 수정은 합격, UI 레이어 U-1~U-6은 별도 수정 권장. 코드 수정은 본 검토에서 미수행(검토 전용).*
