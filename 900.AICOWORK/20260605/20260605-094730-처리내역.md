# 20260605-094730 처리내역 — ai-sarangbang 자동대화 개선·정본 정합·Ollama 연결 보강 + 다른 PC 트러블슈팅

## 세션 목적
"작업 어떻게 진행됐나" 확인에서 출발 → ai-sarangbang(AI+사람 턴제 채팅방) 라이브 검증 중 발견된 이슈를 연쇄 수정. 자동대화 순서·라운드 표시·정본(221/222) 정합·Ollama 연결 보강. 마지막에 다른 PC 실행 트러블슈팅(미해결).

## 작업 흐름 (8건)

### 1. 자동대화 순서: 좌석순 라운드로빈 → 랜덤+연속회피 (라이브 발견)
- 증상(사용자 라이브): 자동대화에서 AI들이 "무조건 순서대로(좌석순)" 발언.
- 진단: 미커밋 자동턴수 수정이 범인 **아님**(diff가 순서 무관). `runAutoTurn`이 C3 구현(6/3)부터 `autoCursor` 좌석순 라운드로빈. 227 랜덤직렬은 사람턴(`runTurn`)에만 적용. 주석 "자동턴 셔플=D-D 후속"으로 인지된 미구현.
- 처방: `pickAutoSpeaker`(직전 화자 제외 후 주입 rng 추첨=연속회피), `autoCursor`→`lastAutoSpeaker`. core 순수성(rng 주입). 테스트=연속회피(인접중복0)+비좌석순(rng 0.99→첫 화자 a3).

### 2. 라운드 종료선 (computeRoundMarks, UI 전용 방식 A)
- 요청: 각 라운드 종료 표시.
- 검토: 죽은 타입 `SystemLine`(원래 turn-end 용도)이 그 자리였으나, **부활 대신 UI 전용** `ui/roundMarks.ts` `computeRoundMarks`(전원 한 바퀴=1라운드, **집합 판정**) 채택. coordinator/history/MD 무변경. Room 렌더 삽입 + `sys.roundEnd` i18n + theme.css.
- 결정(사용자): "전원 한 바퀴 = 1라운드".

### 3. push 완전 검토 (uw-critic 2축) → FIX-FIRST
- 코드 SHIP·발행 CLEAN. 단:
  - **D1(HIGH·문서)**: 정본 221 §R2/§4 D4/용어집 + 222 §4 의사코드가 폐기된 `race/FIFO/Intent 큐` 기술 → 코드(랜덤직렬)와 정면 모순. 두 리뷰어 독립 합의. 미커밋 편집도 본문 미수정.
  - **D2(MED)**: 죽은 타입 `Intent`/`SystemLine` + `types.ts:17` 유령 `collectIntents` 주석.

### 4. D2 정리 + D1 정본 동기화
- D2: `types.ts` Intent/SystemLine 삭제 + L17 주석 정정(collectIntents→셔플). ai-sarangbang 2커밋(types 정리 / 자동+라운드) push.
- D1: **221**(§R2/R3/§4 D4/§5 용어집 → 랜덤 직렬, v2) + **222**(§1·§2·§4 의사코드·§5·§9·§10 → shuffle/collectSpeakers/speakOne/자동/dispose, v4) + **227**(상태 "구현 대기"→완료, §5 정합 체크, D-D, §2.4) 개정. multiaichatting 커밋 push.

### 5. 바퀴 로테이션 (라이브: 라운드 불균등)
- 증상(사용자 라이브 로그): 라운드 2가 7발언(Exaone↔Phi4 6왕복 + Qwen 1). pickAutoSpeaker "직전만 회피"라 3명 중 1명 굶음 → "전원 한 바퀴"가 길어짐. + 라운드4 종료선 위치 이상.
- 처방: `pickAutoSpeaker` **바퀴 로테이션**(이번 바퀴 미발언자 중 추첨, 바퀴 차면 리셋+경계 연속회피, `autoRoundSpoken` Set). `startTurn`에 바퀴 리셋. → 라운드가 정확히 N발언으로 균등.
- 라운드4 진단: roundMarks 회귀 테스트(라이브 시퀀스)로 **로직 정상 확정**(라운드 5개, R4@turnNo14). 라이브 누락은 런타임 빈 응답(화면 거의 안 보임)이 카운트를 민 것으로 추정.
- 커밋: ai-sarangbang `b98e4eb` + multiaichatting 222/221/227 정합 `52808e4`.

### 6. Ollama 연결 보강 (요청: "list 안 보임" 대비)
- 맥락: 사용자 "Ollama 실행했는데 로비에 list 안 보임" 간헐(메모리 "curl 0개→로비 9개 타이밍"과 동일 패턴).
- 4종: ① **URL 입력칸**(비우면 `/ollama` proxy, 입력 시 직접 URL + 영속 `RoomConfig.baseUrl` optional → **v:1 호환**) ② **로딩 타임아웃 8s**(listModels AbortController) ③ **연결 진단**(에러 "연결 시도한 주소"+빈목록 "Ollama 연결됨, 모델 0개"+타임아웃 구분) ④ **재연결**. 코어(ollamaApi/OllamaDriver)는 baseUrl 이미 받아 **무변경**.
- 재검토(uw-critic) FIX-FIRST: **HIGH**(재연결/재시도가 편집 중 슬롯·이름 덮어씀→복원 "비었을 때만"), **MED**(URL 정규화 부재로 `host:port` 상대경로 오인+진단 거짓 보고→`normalizeBaseUrl` scheme 보충·trailing/ 제거). +타임아웃 구분, Lobby/config 회귀 테스트.
- **라이브 직접 테스트**(나라장터 크롬 localhost:5180, Claude in Chrome): UI 표시·진단(연결시도주소=실제)·정규화(localhost:11434→http://localhost:11434)·직접연결+모델로드(localhost CORS 통과)·**편집 보존**(afterAdd=afterReconnect=4)·**영속+정규화**(saved.baseUrl=http://localhost:11434). 전부 통과.
- 커밋: `40ddb24` push.

### 7. README 현행화
- ai-sarangbang README: 바퀴 로테이션·라운드선·로비·연결보강·테스트 97→150·디렉토리(Lobby/App/buildSession/persist/ollamaApi/roundMarks)·모델 예시(qwen3:8b→qwen3.5:4b). 커밋 `06725b2`.
- 220 인덱스 README: 227 v1.1·228 구현완료·각주 정합. 커밋 `f84306c`.

### 8. 다른 PC 트러블슈팅 (미해결, 사용자 결과 대기)
- 증상: 다른 PC(DESKTOP-O644UVD, deepsky) 실행. AI 3개(Qwen2.5vl/Mistral/Codellama) "응답 없음" + `ollama ps` 비어있음.
- 사양: **NVIDIA GeForce + AMD Radeon + 32GB RAM + NVMe SSD = 충분** → 내 초기 "사양 부족" 추정 오류(사과). 7B는 차고 넘침.
- 진단: `ollama ps` 빈 건 추론 미시작. `ollama list` 9개(전부 7b+ 무거움) 정상. **모델 경로 무관**(앱은 API에 모델명만 보냄, 파일은 Ollama가 관리 — list/run 되면 경로 어디든 OK). `ollama run` 미리 불필요(앱이 자동 로드). 유력 원인 = **cold start 타임아웃(60s)** 또는 **Ollama가 NVIDIA 미사용**(GPU 사용률 16%로 낮음, AMD 내장/CPU 폴백 의심).
- 안내: **F12 → Network**에서 `/api/chat` 확인(안 뜸=앱 문제 / pending=cold start 타임아웃 / 빨간에러=연결·모델명). `ollama ps` PROCESSOR가 GPU냐 CPU냐. GPU+cold start면 `config.ollama.idleTimeoutMs` 60→180 push 제안.
- 미해결: 사용자 Network/ps 결과 대기.

## 검증 (게이트)
- vitest 128→131→140→141→144→**150**(23파일) green 누적. tsc 0, vite build 0. core-purity·ui-no-korean 통과.
- 라이브: 자동순서·라운드선(이전 턴), 연결 보강(나라장터 크롬 6종 통과).

## 변경·커밋 (양쪽 origin/main push 완료)
- **ai-sarangbang**: `47cf63d`(자동턴수·랜덤·라운드선) → `2af7323`(죽은타입 제거) → `b98e4eb`(바퀴 로테이션) → `40ddb24`(연결 보강) → `06725b2`(README)
- **multiaichatting**: `6a9129d`(D1 정본 221/222/227) → `52808e4`(바퀴 정합) → `f84306c`(220 README)

## 다음 단계
- **다른 PC 응답없음(최우선)**: F12 Network 결과 → cold start 확정 시 `config.ollama.idleTimeoutMs` 60→180 push. GPU 미사용이면 CUDA/드라이버 점검.
- **`[null]` 사람 이름 버그**: 로비에서 humanName이 `null`로 표시(빈→'나' 폴백 안 됨). buildSession/Lobby humanName 경로 조사.
- **228 본문 연결 보강 반영**: README는 "228=연결 보강 포함 구현완료"로 기술했으나 228 본문 미반영(약한 drift). 한 절 추가하면 완전 정합.
- 백로그: B1 `[나]`/`[이름]` 환각(OllamaDriver 프롬프트 포맷, 사람·AI 둘 다 role:user+[이름]) · B2 D-1 대화 중 모델 교체(driver resolver를 세션 고정→room-reactive) · B3 persist v-bump 마이그레이션.
- dev 서버 5180 background 정리 필요.
- **로컬 라이브 테스트 브라우저 = "나라장터"**(사용자 지정, deviceId는 재연결 시 변동 가능 → 이름으로 select).
