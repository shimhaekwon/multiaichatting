# 20260608 처리내역 — ai-sarangbang 운영수정 + VER-230 클라우드 LLM 확장 구현(신규 repo ai-chatting)

## 세션 목적
1. 다른 PC "응답없음" 원인 검토
2. system 프롬프트/방 이름/자동대화 라벨 수정 + push
3. **VER-230**: 클라우드 LLM을 대화 참여자로 추가 — "API 키 없이 채팅계정으로" 타당성 검토 → 설계 → **신규 폴더/repo 구현**
4. API 키 발급·.env 반영 안내

---

## 1) ai-sarangbang 운영 수정 (`E:\workspace\ai-sarangbang`, origin `shimhaekwon/ai-sarangbang` main)
- **다른 PC 응답없음** = cold start(첫 추론 모델로딩) 추정 → 사용자 "갑자기 잘 됨"으로 해소.
- **"하이"에 90년대 PC통신 사랑방 자기소개** → 원인 = `ollamaSystem`의 system 프롬프트 무대설정(매 요청 주입). 해당 문구 삭제(`config.ts`). 소형모델(Codellama/Mistral)이 배경설정을 인사말에 노출한 것.
- **방 이름 "사랑방"→"AI들과 수다방"** 화면 전부 통일(ko·en): 헤더 `app.title`·입장 `sys.enter`·로비 `lobby.title`(i18n)·브라우저탭(index.html). README·대화저장 MD 제목·테스트 주석의 "사랑방"은 의도적 유지(사용자 선택="화면에 보이는 곳 전부").
- **자동대화 입력칸 "대화 턴" 라벨** 추가: `<label>`로 input 감싸 가시라벨=접근명(기존 aria-label 대체), `theme.css .auto-label`, i18n `auto.turns`('턴 수'→'대화 턴'/'Chat turns').
- **[null] 사람 이름 = 버그 아님**, 로비 "내 이름" 입력값. 빈 값만 '나' 폴백(`buildSession.ts:48`). **이전 메모리의 "폴백 버그" 오기재 정정**.
- 게이트 tsc0·vitest150·build. push: **`1f5cc74`**(프롬프트·이름), **`69000f2`**(라벨).

## 2) VER-230 클라우드 LLM 타당성 검토
- 결론: **"무키=로그인 세션 호출"은 불가** — 로컬 웹앱(확장 아님)에서 httpOnly 인증쿠키·CORS·SameSite·Cloudflare 봇탐지 **4겹 차단**(Vite proxy로도 우회 불가). + OpenAI/Anthropic/Google **3사 ToS 위반** + **2026 Anthropic OpenClaw 실제 계정정지** + v1(multichat) 폐기방식 회귀 + 설계 224 §3(P2=공식API+키프록시) 충돌. **Step1.5 3건 모두 hit**.
- 방향(사용자 결정): **무료 API 키**(Gemini 무료 티어 + OpenRouter 무료 모델) + 키보관 **Vite proxy + .env**(클라이언트 노출 0). "무키"는 막혔으나 "무료"는 가능 — 실질 목적(비용0) 충족.
- 정찰 uw-explorer 3(코드 seam·설계문서/v1 교훈·웹 ToS/무료티어) + **uw-critic 2라운드**.

## 3) 설계 산출물
- 요구사항: `100.START/VER-230-ai-chatting-cloud/{10-검토결과-및-방향, 20-요구사항}`
- 설계: `230.설계산출물-ai-chatting-cloud/{231-설계-아키텍처-데이터-보안-UI, 232-구현계획, README}` (220 미러링 신규 폴더)
- **uw-critic 1차**(H1 .gitignore에 .env없음·H2 persist 마이그레이션 불가+Lobby v:1·H3 라벨환각·M sse/role/diag/model) 전량 반영 → 231 §7
- **통합 UI/UX 변경(사용자 요청 "로컬+클라우드 함께 보기")**: provider 2단계 → **통합 optgroup 드롭다운 + 상태배지**, **Ollama-off 클라우드-only 입장(게이트완화)** → FR-C02 재정의·FR-C14 신규
- **uw-critic 2차**(R2-1 배지헬스 라이브확인: OpenRouter `/models`는 공개→키프로브 `/api/v1/key`로 / R2-2 스키마bump 테스트반전 / R2-3 gone 판정 / R2-4 provider::model value / R2-5 모델ID '확정' 철회 / R2-6 게이트 loading / R2-7 dangling FR-C15) 전량 반영 → 231 §8

## 4) 구현 (`E:\workspace\ai-sarangbang-cloud` → **github `shimhaekwon/ai-chatting`**)
- 베이스라인: 기존 복사본(원본과 src 동일·게이트 통과·.git 없음) 확인.
- **config.ts**: `Provider`/`CloudProvider`·`AiSlot.provider`·`RoomConfig v2`·`cloud` 블록(gemini=`gemini-2.5-flash`,health `/models` · openrouter=`openrouter/free`,health `/key`)
- **drivers/CloudDriver.ts**(신규)+test: OpenAI 호환 `chat/completions` SSE→토큰·signal abort·빈응답 throw·`mergeSameRole`(FR-C13). `buildOllamaMessages`([이름] 포맷) 재사용
- **buildSession.ts**: provider별 라우팅 드라이버(슬롯 provider로 발화마다 분기, 전부 stripLeadingSelfLabel) — **core/coordinator 무수정**
- **persist.ts**: `SCHEMA_V=2` + `migrate` v1→v2(provider='ollama' 주입, 기존 방 보존) + `isValidSlot` provider + `reconcileModels` ollama-only (+test)
- **Lobby.tsx**: 통합 optgroup(로컬+클라우드, value `provider::model`) · 키-요구 헬스 배지(✓/🔒, unmount 가드) · 게이트완화(클라우드-only 입장) · gone 판정 provider별 · think 리셋 (+test 클라우드-only/optgroup)
- **vite.config.ts**: `/gemini`·`/openrouter` proxy(server+preview) + `loadEnv` 키 헤더주입(VITE_ 미접두→번들 0). **.gitignore에 `.env` 추가**(critic must-fix) · `.env.example`
- i18n·theme.css 배지/그룹 라벨
- **코드 uw-critic**: .gitignore `.env` 누락(HIGH must-fix, 재적용 누락분 복구) · 헬스 unmount setState 가드(MED) 수정. 키없는 방 입장=FR-C14 설계결정(유지)·배지 실패경로 테스트·모델ID는 follow-up.
- 게이트: **tsc0 · vitest 163(150+13: CloudDriver8·persist2·Lobby2·buildSession1) · vite build OK**. ui-no-korean·core-purity 통과.
- git init main + commit **`e4d6d2e`**(67파일) → 새 repo push(`shimhaekwon/ai-chatting` main). ※ gh 부재 + `git credential fill` stdin 샌드박스 차단(파이프/.NET/raw바이트/cmd 전부 실패) → **사용자가 repo 직접 생성**, push는 캐시 자격증명으로 성공.

## 검증
- ai-sarangbang-cloud: **tsc0 · vitest 163/163 · build OK**.
- **라이브(클라우드 실발화)는 미완 = 사용자 키 필요**. .env에 무료키 입력 → `npm run dev` → 로비 배지 ✓ → 발화 확인이 남음.

## 변경 파일
- **ai-sarangbang**: src/app/config.ts, src/i18n/index.ts, index.html, src/ui/Room.tsx, src/ui/theme.css
- **ai-sarangbang-cloud(신규/변경)**: src/app/config.ts·persist.ts(+test)·buildSession.ts(+test), src/drivers/CloudDriver.ts(+test), src/ui/Lobby.tsx(+test)·theme.css, src/i18n/index.ts, vite.config.ts, .gitignore, .env.example, package.json
- **설계**: 100.START/VER-230-ai-chatting-cloud/*, 230.설계산출물-ai-chatting-cloud/* (multiaichatting repo, 미push — 세션 중 작성만)

## 다음 단계
1. **사용자 라이브 검증**: `.env` 키 입력 후 프로젝트 폴더에서 `npm run dev`(Git Bash는 `--prefix` 백슬래시 주의 → 폴더 안에서 `npm run dev`). 로비 Gemini/OpenRouter 배지 ✓ → 발화.
2. **⚠️ Gemini 키 형식 점검**: 사용자가 넣은 값이 `AQ.`로 시작(통상 Gemini API키는 `AIza`) → OAuth 토큰 의심. 401/🔒 시 AI Studio "Create API key"의 `AIza…` 재발급. OpenRouter 키(`sk-or-v1-…`)는 정상 형식 → 먼저 OpenRouter로 테스트 권장.
3. 설계문서(multiaichatting) push 여부 사용자 확인.
4. follow-up: prod 키 백엔드(설계 P2 합류)·유료 직접연동(동일 CloudDriver)·클라우드 모델목록 동적화·모델ID 현행 재확인·멀티유저.

## 6-6 원칙 회고
- **①빠짐없이**: 기술·약관·v1·설계충돌·FR·비평 갭 전수
- **②틀림없이**: 코드 사실 직접 확인(.gitignore/persist/Lobby v:1) + 웹/라이브 엔드포인트 확인으로 배지 거짓전제(OpenRouter /models 공개) 제거
- **③더함없이**: "설계 먼저"·"통합 UI"·git 전략 등 사용자 선택 범위만, follow-up 분리
- **④다름없이**: AgentDriver seam·Vite proxy·VER-003 문서관례·[이름] 포맷 미러
- **⑤step by step**: 검토→설계→2×critic→베이스라인→슬라이스 구현→게이트→코드critic→push
- **⑥no guess**: 방향/키보관/범위/UI패턴/Ollama-off/git 전략 모두 AskUserQuestion으로 확정
- **⑦challenge**: 무키 불가+ToS+계정정지+pivot충돌 alert, Gemini 키 형식 사전 경고, 키없는 방 입장은 설계결정 명시

## 보안 메모
클라우드 키는 `.env`(gitignore 확인됨)에만 — 커밋·번들 노출 0. (키가 대화 입력 과정에 노출됐으면 추후 재발급 권장.)
