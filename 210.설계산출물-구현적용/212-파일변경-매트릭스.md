# 212. 파일 변경 매트릭스

> ChatHub(`E:\workspace\multichat\`) 파일/폴더별 처리. **분류**: 재사용(로직 보존; 제거모듈 import는 정리될 수 있음 — [M-1] 노트 §8 말미) · 개조(로직 수정) · 신규(생성) · 제거(삭제).

## 1. 진입점 / 빌드 / 매니페스트

| 경로 | 분류 | 내용 |
|------|------|------|
| `manifest.config.ts` | 개조 | host_permissions 6종(chatgpt.com/claude.ai/perplexity.ai/gemini.google.com/chat.deepseek.com/grok.com), content_scripts에 `dom-adapter.ts`를 **6사이트** 매칭으로 추가, declarative_net_request rules **전부 제거**(전 봇 DOM이라 불필요), commands 유지 |
| `vite.config.ts`, `tsconfig.json`, `package.json` | 개조 | **`yaml`** 의존성 추가(`yarn add yaml`; 216은 `import { stringify } from 'yaml'` 사용). 제거 봇 관련 미사용 의존성(`@google/generative-ai`·`plausible-tracker`·`@sentry/*` 등) 정리 |
| `app.html` | 재사용 | **메인 진입점**(`main.tsx`→router `/`=`MultiBotChatPanel` 그리드). action 클릭 시 이 탭이 열림(`background openAppPage`) [N-1] |
| `sidepanel.html`/`sidepanel.tsx` | **제거** | [222 §1 확정] app.html 탭이 메인 → 사이드패널 폐기. `sidepanel.tsx`·`SidePanelPage.tsx` 삭제 + manifest `side_panel` 키·`sidePanel` 권한 제거 |

## 2. 봇 레이어 (`src/app/bots/`)

| 경로 | 분류 | 내용 |
|------|------|------|
| `abstract-bot.ts` | 재사용(주의) | 공통 계약 유지. **[M-1]** 단 `import { Sentry } from '~services/sentry'`(L1)·`Sentry.captureException`(L40) 의존 → **`sentry.ts`는 삭제 말고 no-op 유지**(§6) 또는 이 import/호출 2곳 제거. 미처리 시 전 봇 빌드 깨짐 |
| `dom-adapter-bot.ts` | **신규** | DOM 어댑터 공통 베이스(217 §3.1) |
| `index.ts` | 개조 | `BotId` 6종, `createBotInstance` 6 case 전부 `DomAdapterBot` 반환 |
| `chatgpt/` | **재작성** | 내부 API → `ChatGPTDomBot`(DOM) |
| `claude/` | **재작성** | 내부 API → `ClaudeDomBot`(DOM) |
| `perplexity/` | **재작성** | 내부 API → `PerplexityDomBot`(DOM) |
| `gemini/` | **신규** | `GeminiDomBot`(gemini.google.com) |
| `deepseek/` | **신규** | `DeepSeekDomBot`(chat.deepseek.com) |
| `grok/` | **신규**(재작성) | `GrokDomBot`(grok.com), Twitter API 폐기 |
| `chatgpt-webapp/`,`claude-web/`,`perplexity-web/` | **제거** | 내부 API 본체 — all-DOM 전환으로 미사용 |
| `bing/`,`bard/`,`poe/`,`pi/`,`qianwen/`,`xunfei/`,`baichuan/`,`lmsys/`,`gradio/`,`openrouter/`,`gemini-api/`,`chatgpt-api/`,`chatgpt-azure/`,`claude-api/`,`perplexity-api/` | 제거 | 범위 밖 |

## 3. 상태 (`src/app/state/`)

| 경로 | 분류 | 내용 |
|------|------|------|
| `state/index.ts` | 개조 | premium/license/discount/releaseNotes atom 제거, theme/sidebar atom 유지, `chatFamily` 유지(슬롯 키로 사용) |
| `state/round.ts` | **신규** | `roundSessionAtom`, `selectedLLMsAtom`, `secretChatAtom`(→214) |

## 4. 훅 (`src/app/hooks/`)

| 경로 | 분류 | 내용 |
|------|------|------|
| `use-chat.ts` | 재사용/소개조 | 슬롯별 스트리밍 머신 그대로, plausible 호출 제거 |
| `use-round-engine.ts` | **신규** | 라운드 상태머신 오케스트레이션(→215) |
| `use-enabled-bots.ts` | 개조 | LLM풀(6종) 반환으로 단순화 |
| `use-premium.ts`, `use-purchase-info.ts` | 제거 | 수익화 |
| `use-user-config.ts` | 재사용/소개조 | 6종 설정만 |

## 5. 유틸 (`src/app/utils/`, `src/utils/`)

| 경로 | 분류 | 내용 |
|------|------|------|
| `utils/sse.ts`, `utils/stream-async-iterable.ts`, `utils/encoding.ts`, `utils/errors.ts` | 재사용 | 봇 스트리밍 공통 |
| `app/utils/permissions.ts`, `app/utils/image-compression/*`, `app/utils/markdown.ts` | 재사용 | |
| `app/utils/context-builder.ts` | **신규** | self-exclusion YAML(→216) |
| `app/utils/md-export.ts` | **신규** | MD 산출(→214 §4, 219) |
| `app/utils/export.ts` | 재사용(소개조) | browser-fs-access 활용. **[M-1]** `~app/plausible` `trackEvent` 2곳(L14·L38) 제거 |

## 6. 서비스 (`src/services/`)

| 경로 | 분류 | 내용 |
|------|------|------|
| `proxy-fetch.ts` | **제거** | 내부 API 폐기(전 봇 DOM) → 미사용 |
| `user-config.ts` | 개조 | 6종 무키 설정만, API키/모드 필드 제거 |
| `chat-history.ts` | 재사용 | 셀 "기록" 기능 |
| `storage/language.ts` | 재사용 | i18n |
| `premium.ts`,`lemonsqueezy.ts`,`prompts.ts`,`agent/*`,`storage/{open-times,token-usage}.ts`,`release-notes.ts` | 제거 | 범위 밖 |
| `sentry.ts` | **개조(no-op)** | **[M-1]** `abstract-bot.ts`가 import → **삭제 금지**. `Sentry.captureException`/`Sentry.init`을 no-op 스텁으로 두거나 abstract-bot의 import/호출을 제거. (`@sentry/*` 의존성도 제거 가능) |
| `server-api.ts` | 개조/제거 | premium/discount 제거. 다른 용도 없으면 제거 |
| `session-store.ts` | **신규** | `chrome.storage.session` 라운드 스냅샷(→214 §5) |

## 7. 백그라운드 / 콘텐츠 스크립트

| 경로 | 분류 | 내용 |
|------|------|------|
| `background/index.ts` | 개조 | 명령(`open-app`)+action 클릭 시 **app.html 탭 오픈**(`openAppPage`, 사이드패널 자동오픈 아님) 유지, DOM 봇용 탭 메시지 라우팅 추가(→213), **source/twitter 제거 시 호출처도 정리**(`trackInstallSource()`·`readTwitterCsrfToken`+twitter 메시지핸들러) [N-4]. `storage.session.setAccessLevel`(:9) 유지 |
| `background/source.ts`, `background/twitter-cookie.ts` | 제거 | 분석/Grok-Twitter |
| `content-script/chatgpt-inpage-proxy.ts` | **제거** | proxyFetch 폐기(전 봇 DOM) |
| `content-script/dom-adapter.ts` | **신규** | **6종 공통** 콘텐츠 스크립트(selector 주입·관찰·STOP, →217), 6사이트 주입 |
| `content-script/adapter-config.json` | **신규** | 6종 selector 단일 출처(version 필드, →217 §3.2) |
| `rules/*.json` + `declarative_net_request` | **제거** | 전 봇 DOM이라 DNR 불요 → 5룰(bing/ddg/qianwen/baichuan/pplx) 전부 삭제 + `declarativeNetRequestWithHostAccess` 권한 제거 [222 §2·§3] |

## 8. 컴포넌트 / 페이지

| 경로 | 분류 | 내용 |
|------|------|------|
| `components/Chat/{ChatMessageList,ChatMessageCard,MessageBubble,ChatbotName}.tsx` | 재사용 | 셀 표시·헤더 |
| `components/Chat/ChatMessageInput.tsx` | **개조** | **[M-1]** `~services/prompts`·`../PromptCombobox`·`../PromptLibrary/Dialog`·`~app/plausible` 의존(L20·21·23·24) → **프롬프트 라이브러리/콤보박스/`trackEvent` 제거**. `mode==='full'`의 GoBook·Combobox 블록(L163~194) 삭제. 미수정 시 prompts/PromptLibrary 제거로 빌드 깨짐 |
| `components/Chat/ConversationPanel.tsx` | 개조 | **[G3]** roundMode 추가·WebAccessCheckbox 제거·🔒 추가·trackEvent 제거 |
| `components/Chat/WebAccessCheckbox.tsx` | 제거 | **[G11]** 웹접근 기능 미사용 |
| `components/Chat/LayoutSwitch.tsx` | 제거/개조 | 2×3 고정이면 제거(설정바로 대체) |
| `components/Markdown/*` | 재사용 | 마크다운 렌더 |
| `components/Share/*` | 개조(소) | **[M-1]** `ShareGPTView.tsx`·`MarkdownView.tsx`의 `~app/plausible` `trackEvent` 제거 |
| `components/History/Dialog.tsx` | **개조** | **[M-1]** `~app/hooks/use-premium` 의존(L8·L39, 프리미엄 게이트) 제거 → 기록 항상 허용. `History/*` 나머지(Content/ChatMessage)는 재사용 |
| `components/Sidebar/*` | 개조 | LLM풀 체크박스(6종)+전체선택(→218) |
| `components/Premium/*`, `components/PromptLibrary/*`, `components/Modals/ReleaseNotesModal.tsx` | 제거 | 범위 밖 |
| `components/Settings/*` | 대부분 제거 | API키 패널 제거. 어댑터 selector 설정만 신규(선택) |
| `components/SettingsBar.tsx` | **신규** | 라운드/토큰 select + 세션 종료(→218) |
| `components/SecretChatOverlay.tsx` | **신규** | 비밀채팅(→218) |
| `components/SaveSessionDialog.tsx` | **신규** | MD 저장 다이얼로그(→218) |
| `pages/MultiBotChatPanel.tsx` | 개조 | 2×3 라운드 그리드 + 엔진 연동(→218) |
| `pages/SidePanelPage.tsx` | **제거** | [222 §1] 사이드패널 폐기로 미사용 (메인 = app.html `MultiBotChatPanel`) |
| `pages/SettingPage.tsx` | 대폭 개조/제거 | 최소화 |
| `pages/PremiumPage.tsx`, `pages/SingleBotChatPanel.tsx` | 제거 | 단일봇 페이지 불필요 |
| `router.tsx` | 개조 | premium 라우트 제거, 메인/설정만 |
| `consts.ts` | 개조 | `CHATBOTS` 6종으로, `Layout` 단순화 |
| `i18n/*` | 재사용/개조 | 신규 문구 키 추가 |

> **[M-1] `~app/plausible`(trackEvent/plausible) 제거 호출처 전수** — `plausible.ts` 삭제 시 다음이 모두 깨지므로 G10 grep으로 일괄 제거(call site만 삭제, 로직 유지): `hooks/use-chat.ts`(L3), `components/Chat/ConversationPanel.tsx`(L10), `components/Chat/ChatMessageInput.tsx`(L20), `components/Share/{ShareGPTView,MarkdownView}.tsx`(L2), `components/ThemeSettingModal/index.tsx`(L9), `pages/MultiBotChatPanel.tsx`(L13), `app/utils/export.ts`(L3), `sidepanel.tsx`(L10), `main.tsx`(L6, `import { plausible }`). **`main.tsx`/`sidepanel.tsx`는 plausible 초기화 호출도 제거.**
>
> **[M-1] "재사용=수정없음" 재정의**: 본 매트릭스에서 `재사용`은 "로직 보존"을 뜻하며, 제거대상 모듈(plausible/prompts/PromptLibrary/premium/sentry/server-api/open-times) import가 있는 파일은 **해당 import만 정리하는 소개조**가 동반된다(빌드 그린 조건). 위 [M-1] 표·노트가 그 전수 목록이다.

## 9. 신규 파일 경로 목록 (생성 체크리스트)

```
src/app/state/round.ts
src/app/hooks/use-round-engine.ts
src/app/utils/context-builder.ts
src/app/utils/md-export.ts
src/services/session-store.ts
src/content-script/dom-adapter.ts
src/content-script/adapter-config.json
src/app/bots/dom-adapter-bot.ts
src/app/bots/chatgpt/index.ts         (재작성: 내부API→DOM)
src/app/bots/claude/index.ts          (재작성: 내부API→DOM)
src/app/bots/perplexity/index.ts      (재작성: 내부API→DOM)
src/app/bots/gemini/index.ts
src/app/bots/deepseek/index.ts
src/app/bots/grok/index.ts            (재작성)
src/app/components/SettingsBar.tsx
src/app/components/SecretChatOverlay.tsx
src/app/components/SaveSessionDialog.tsx
src/app/components/RoundActionButton.tsx  (신규 — 222 §10; RoundProgress는 SettingsBar 내부 소컴포넌트)
src/types/round.ts                    (신규: 라운드 타입 — 214 §1)
src/assets/logos/deepseek.png         (신규 로고 — CHATBOTS[deepseek].avatar, 217 §1)
```
